---
phase: 03-operable-fixes
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - nextjs-app/shared/patterns/SiteHeader/MobileDrawer.tsx
  - tests/a11y/operable/focus-trap.spec.ts
autonomous: true

must_haves:
  truths:
    - "User can escape from any focused element (no keyboard traps)"
    - "Modal traps focus correctly and releases on close"
    - "Skip link moves focus to main content"
    - "Focus returns to trigger element when modal/drawer closes"
  artifacts:
    - path: "tests/a11y/operable/focus-trap.spec.ts"
      provides: "Focus trap and skip link verification tests"
      min_lines: 80
  key_links:
    - from: "Modal.tsx"
      to: "main content"
      via: "inert attribute"
      pattern: "setAttribute.*inert"
    - from: "SkipLink.tsx"
      to: "NextLayout.tsx"
      via: "#main-content href"
      pattern: "id=\"main-content\""
---

<objective>
Verify focus trap behavior in Modal, ChatWidget, and MobileDrawer. Verify skip link functionality. Fix MobileDrawer if focus trap is incomplete.

Purpose: Ensure OPER-02 (no keyboard traps) and OPER-03 (skip links work) are satisfied.
Output: Verified focus trap behavior + skip link tests + any necessary fixes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-operable-fixes/03-RESEARCH.md
@nextjs-app/shared/components/Modal/Modal.tsx
@nextjs-app/shared/components/ChatWidget/ChatWidget.tsx
@nextjs-app/shared/patterns/SiteHeader/MobileDrawer.tsx
@nextjs-app/shared/components/SkipLink/SkipLink.tsx
@nextjs-app/shared/components/NextLayout/NextLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create focus trap and skip link tests</name>
  <files>tests/a11y/operable/focus-trap.spec.ts</files>
  <action>
Create Playwright tests for focus trap behavior and skip link functionality:

**OPER-02: No Keyboard Traps**

1. **Modal focus trap:**
   - Open modal via trigger button
   - Verify focus moves to first focusable element inside modal
   - Tab repeatedly - verify focus stays within modal
   - Press Escape - verify modal closes
   - Verify focus returns to the trigger button

2. **ChatWidget focus trap:**
   - Click chat toggle to open
   - Verify focus moves to input textarea
   - Tab - verify focus cycles within chat panel
   - Press Escape - verify chat closes
   - Verify focus returns to toggle button

3. **MobileDrawer focus trap (mobile viewport):**
   - Set viewport to 375x667 (mobile)
   - Click hamburger menu
   - Verify focus moves inside drawer
   - Press Escape - verify drawer closes
   - Verify focus returns to hamburger button

**OPER-03: Skip Links**

4. **Skip link functionality:**
   - Navigate to home page
   - Press Tab once
   - Verify skip link is visible
   - Press Enter on skip link
   - Verify focus moves to `#main-content` element
   - Verify skip link scrolls to main content

**Pattern:** Use `page.keyboard.press("Tab")`, `page.keyboard.press("Escape")`, and `expect(locator).toBeFocused()`
  </action>
  <verify>
```bash
npm run dev &
npx playwright test tests/a11y/operable/focus-trap.spec.ts --reporter=list
```
All tests should pass.
  </verify>
  <done>Focus trap tests pass for Modal, ChatWidget, MobileDrawer. Skip link test verifies focus moves to main content.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix MobileDrawer focus management</name>
  <files>nextjs-app/shared/patterns/SiteHeader/MobileDrawer.tsx</files>
  <action>
Research findings indicate MobileDrawer has Escape handler (lines 91-96) but unclear focus trap status.

**Verify:**
1. Does MobileDrawer set `inert` on main content when open?
2. Does focus move to first element inside drawer on open?
3. Does focus return to hamburger button on close?

**If any of these are missing, add:**

1. **Focus on open:** Store previous active element, move focus to first item in drawer
2. **Inert on main:** Apply `inert` attribute to main content to prevent focus escape
3. **Focus return on close:** Return focus to stored element

Follow the pattern from Modal.tsx:
```tsx
// Store previous focus
previousActiveElement.current = document.activeElement;

// Set inert on main content
mainContent.setAttribute("inert", "");

// On close: remove inert, restore focus
mainContent.removeAttribute("inert");
previousActiveElement.current?.focus();
```

**If MobileDrawer already has proper focus management:** Document the existing implementation in the task notes, no code changes needed.
  </action>
  <verify>
```bash
npm run dev &
npx playwright test tests/a11y/operable/focus-trap.spec.ts -g "MobileDrawer" --reporter=list
```
MobileDrawer focus trap test should pass.
  </verify>
  <done>MobileDrawer has proper focus trap behavior: focus moves to drawer on open, Escape closes and returns focus to trigger.</done>
</task>

</tasks>

<verification>
1. Modal focus trap test passes (focus cycles within, Escape closes, focus returns)
2. ChatWidget focus trap test passes (focus in input, Escape closes, focus returns to toggle)
3. MobileDrawer focus trap test passes (focus trapped, Escape closes, focus returns to hamburger)
4. Skip link test passes (visible on Tab, Enter navigates to #main-content)
5. No unintentional keyboard traps exist on any page
</verification>

<success_criteria>
- Focus trap tests exist at `tests/a11y/operable/focus-trap.spec.ts`
- All focus trap tests pass for Modal, ChatWidget, MobileDrawer
- Skip link test passes
- MobileDrawer has focus management matching Modal pattern (if fix needed)
- OPER-02 and OPER-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-operable-fixes/03-03-SUMMARY.md`
</output>
