---
phase: 05-security-testing
plan: 01
type: execute
depends_on: []
files_modified:
  - app/__tests__/security-rate-limiting.test.ts
  - app/__tests__/security-timing-safe.test.ts
  - app/__tests__/security-cors.test.ts
---

<objective>
Add automated tests for security features implemented in Phases 2-4: timing-safe password comparison, rate limiting, and CORS origin validation.

Purpose: Prevent regressions in critical security features. Tests serve as executable documentation of security requirements.
Output: Security test suite with tests for rate limiting behavior, timing-safe comparison, and CORS validation.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries (security features implemented):
@.planning/phases/02-timing-attack-fixes/02-01-SUMMARY.md
@.planning/phases/03-rate-limiting/03-01-SUMMARY.md
@.planning/phases/04-cors-hardening/04-01-SUMMARY.md

# Key source files to test:
@app/api/download-cv/route.ts
@app/api/save-contact/route.ts
@app/api/chat-shared.ts
@app/api/test-health/runs/route.ts

# Testing patterns:
@.planning/codebase/TESTING.md

**Tech stack available:** Vitest 4.0.16, @testing-library/react, jest-dom matchers
**Established patterns:**
- Tests colocated at `app/__tests__/` for integration tests
- `describe`/`it` structure with `vi` mocking
- Factory pattern for test data

**Constraining decisions:**
- Phase 2: Use constantTimeCompare pattern with crypto.timingSafeEqual
- Phase 3: In-memory rate limiting (3 req/15min for save-contact, 5 req/15min for download-cv)
- Phase 4: Use createCorsHeaders() for all CORS, validate against allowedOrigins

**Security patterns to test:**
1. `constantTimeCompare(a, b)` - Timing-safe string comparison
2. Rate limit buckets - Map-based with window expiry
3. `createCorsHeaders(origin)` / `resolveAllowedOrigin(origin)` - Origin validation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create timing-safe comparison tests</name>
  <files>app/__tests__/security-timing-safe.test.ts</files>
  <action>
Create unit tests for the constantTimeCompare function pattern used in download-cv and test-health routes.

Test cases:
1. Returns true for identical strings
2. Returns false for different strings of same length
3. Returns false for strings of different lengths
4. Returns false for empty vs non-empty strings
5. Handles special characters (unicode, emojis)

Note: We cannot unit test the timing characteristics (that requires statistical analysis). We test correctness only - that the function produces the same boolean results as direct equality but uses the timing-safe implementation.

Import the function from the route file or recreate the identical implementation in tests to verify the pattern works correctly.
  </action>
  <verify>npm test -- app/__tests__/security-timing-safe.test.ts passes</verify>
  <done>All timing-safe comparison test cases pass, covering identity, difference, length mismatch, and edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Create rate limiting behavior tests</name>
  <files>app/__tests__/security-rate-limiting.test.ts</files>
  <action>
Create unit tests for the rate limiting pattern used in save-contact and download-cv routes.

Test the rate limit function logic (not the full API route):
1. First request is not rate limited
2. Requests within limit are allowed (e.g., 3 for save-contact)
3. Request at limit (4th) is rate limited
4. Window reset after expiry allows new requests
5. Different IPs have independent buckets

Use vi.useFakeTimers() to test window expiry behavior. Create a minimal implementation of the rate limit function in tests that mirrors the production code pattern.

Structure:
```typescript
const RATE_LIMIT_WINDOW_MS = 900_000; // 15 minutes
const RATE_LIMIT_MAX = 3;
const buckets = new Map<string, { count: number; windowStart: number }>();

function rateLimit(key: string): boolean {
  // ... same logic as production
}
```

Test this function directly to verify the algorithm works correctly.
  </action>
  <verify>npm test -- app/__tests__/security-rate-limiting.test.ts passes</verify>
  <done>All rate limiting tests pass, verifying limit enforcement, window expiry, and IP isolation</done>
</task>

<task type="auto">
  <name>Task 3: Create CORS origin validation tests</name>
  <files>app/__tests__/security-cors.test.ts</files>
  <action>
Create unit tests for CORS validation functions from chat-shared.ts:
- resolveAllowedOrigin(origin)
- createCorsHeaders(origin)

Test cases for resolveAllowedOrigin:
1. Returns origin when in allowedOrigins list (e.g., "https://digitaltableteur.com")
2. Returns origin for localhost variants (localhost, 127.0.0.1, ::1)
3. Returns origin for private network IPs (192.168.x.x, 10.x.x.x)
4. Returns fallback (first allowed origin) for unknown origins
5. Returns fallback for null/undefined origin
6. Returns fallback for malicious origins (attacker.com)

Test cases for createCorsHeaders:
1. Returns correct headers with valid origin
2. Returns headers with fallback origin for invalid origin
3. Includes Vary: Origin header
4. Includes correct methods and max-age

Import directly from chat-shared.ts and test the exported functions.
  </action>
  <verify>npm test -- app/__tests__/security-cors.test.ts passes</verify>
  <done>All CORS tests pass, verifying origin validation and header generation</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm test -- app/__tests__/security-*.test.ts` passes all tests
- [ ] `npm run typecheck` passes
- [ ] No lint errors in test files
- [ ] Tests cover all security features from Phases 2-4
</verification>

<success_criteria>

- All 3 test files created
- All tests pass
- Tests cover: timing-safe comparison, rate limiting, CORS validation
- No TypeScript or lint errors
- Phase 5 complete (milestone finished)
</success_criteria>

<output>
After completion, create `.planning/phases/05-security-testing/05-01-SUMMARY.md`:

# Phase 5 Plan 01: Security Testing Summary

**[One-liner summary of what was tested and coverage achieved]**

## Accomplishments

- [Test files created]
- [Security features covered]
- [Number of test cases]

## Files Created/Modified

- `app/__tests__/security-timing-safe.test.ts` - Description
- `app/__tests__/security-rate-limiting.test.ts` - Description
- `app/__tests__/security-cors.test.ts` - Description

## Decisions Made

[Key decisions about test approach, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete. Security hardening milestone finished.
</output>
