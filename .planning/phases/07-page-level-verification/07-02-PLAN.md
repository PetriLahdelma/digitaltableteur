---
phase: 07-page-level-verification
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/a11y/page-verification/core-pages.spec.ts
  - tests/a11y/page-reports/home/home-report.md
  - tests/a11y/page-reports/about/about-report.md
  - tests/a11y/page-reports/work/work-report.md
  - tests/a11y/page-reports/blog/blog-report.md
  - tests/a11y/page-reports/contact/contact-report.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Home page passes axe-core audit in all 4 themes and 3 languages (12 combinations)"
    - "About page passes audit in all theme/language combinations"
    - "Work index page passes audit in all theme/language combinations"
    - "Blog index page passes audit in all theme/language combinations"
    - "Contact page passes audit in all theme/language combinations"
  artifacts:
    - path: "tests/a11y/page-verification/core-pages.spec.ts"
      provides: "Playwright tests for 5 core pages"
      min_lines: 100
    - path: "tests/a11y/page-reports/home/home-report.md"
      provides: "Full audit report for home page"
      contains: "Page Verification Report: Home"
    - path: "tests/a11y/page-reports/contact/contact-report.md"
      provides: "Full audit report for contact page"
      contains: "Page Verification Report: Contact"
  key_links:
    - from: "tests/a11y/page-verification/core-pages.spec.ts"
      to: "tests/a11y/page-verification/helpers/audit-page.ts"
      via: "import"
      pattern: "import.*auditPageWithThemeAndLanguage"
    - from: "tests/a11y/page-verification/core-pages.spec.ts"
      to: "tests/a11y/page-verification/helpers/page-registry.ts"
      via: "import"
      pattern: "import.*corePages"
---

<objective>
Verify the 5 core public pages pass WCAG 2.1 AA automated audit.

Purpose: Satisfy PAGE-01 through PAGE-05 requirements for automated verification. These are the highest-traffic pages and must pass all theme/language combinations.

Output:
- `core-pages.spec.ts` - Playwright test suite
- Individual page reports in `tests/a11y/page-reports/`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-page-level-verification/07-CONTEXT.md
@.planning/phases/07-page-level-verification/07-01-SUMMARY.md
@tests/a11y/page-verification/helpers/audit-page.ts
@tests/a11y/page-verification/helpers/report-generator.ts
@tests/a11y/page-verification/helpers/page-registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core-pages.spec.ts test suite</name>
  <files>tests/a11y/page-verification/core-pages.spec.ts</files>
  <action>
Create Playwright test file that:

1. Imports from helpers:
```typescript
import { auditPageWithThemeAndLanguage, themes, languages, AuditResult } from "./helpers/audit-page";
import { generatePageReport } from "./helpers/report-generator";
import { corePages } from "./helpers/page-registry";
```

2. Uses test.describe for "Core Pages Verification"

3. For EACH core page (5 pages), create a test that:
   - Iterates through all 4 themes
   - For each theme, iterates through all 3 languages
   - Calls auditPageWithThemeAndLanguage()
   - Collects all 12 results
   - Asserts ZERO violations for each combination
   - Generates markdown report using generatePageReport()
   - Writes report to tests/a11y/page-reports/{pageName}/{pageName}-report.md

4. Use fs.mkdirSync with recursive:true to create report directories

5. Test naming: "should pass audit in all themes and languages: {pageName}"

6. Add test.afterAll hook that writes a summary report

DO NOT use test.describe.parallel within themes/languages - run combinations sequentially within each page test to avoid race conditions on DOM manipulation.

The 5 page tests CAN run in parallel (different browser contexts).
  </action>
  <verify>
```bash
# Verify file exists and has correct structure
grep -c "test(" tests/a11y/page-verification/core-pages.spec.ts
# Should show 5 (one per core page) or 6 (with summary)

# Run type check
npx tsc --noEmit tests/a11y/page-verification/core-pages.spec.ts
```
  </verify>
  <done>core-pages.spec.ts created with tests for all 5 core pages</done>
</task>

<task type="auto">
  <name>Task 2: Run core page audits and verify zero violations</name>
  <files>
tests/a11y/page-reports/home/home-report.md
tests/a11y/page-reports/about/about-report.md
tests/a11y/page-reports/work/work-report.md
tests/a11y/page-reports/blog/blog-report.md
tests/a11y/page-reports/contact/contact-report.md
  </files>
  <action>
Execute the test suite:

```bash
npx playwright test tests/a11y/page-verification/core-pages.spec.ts --reporter=list
```

Expected:
- All 5 tests pass (60 total theme/language combinations: 5 pages x 4 themes x 3 languages)
- Report files created in tests/a11y/page-reports/

If any violations found:
1. Document in summary
2. Check if violation is from third-party content (exception allowed)
3. Check if violation was supposed to be fixed in Phase 2-6
4. Create issue for gap closure if needed

DO NOT fail the plan if tests pass. If tests fail with violations, document them - Phase 7 is verification, not fixing.
  </action>
  <verify>
```bash
# Verify reports were generated
ls -la tests/a11y/page-reports/*/
# Should show 5 directories with report files

# Check for PASS status in reports
grep -l "Status: PASS" tests/a11y/page-reports/*/*.md | wc -l
# Should be 5 (all pass)
```
  </verify>
  <done>All 5 core pages pass automated audit, reports generated with zero violations</done>
</task>

</tasks>

<verification>
1. core-pages.spec.ts exists and compiles
2. All 5 core page tests pass (60 combinations)
3. Report files exist for all 5 pages
4. Each report shows "Status: PASS" or documents exceptions
5. Requirements satisfied: PAGE-01 (Home), PAGE-02 (About), PAGE-03 (Work index), PAGE-04 (Blog index), PAGE-05 (Contact)
</verification>

<success_criteria>
- Zero axe-core violations across all 60 theme/language/page combinations
- Reports document all results with timestamps
- If exceptions exist, they are third-party embeds or documented justifications
</success_criteria>

<output>
After completion, create `.planning/phases/07-page-level-verification/07-02-SUMMARY.md`
</output>
