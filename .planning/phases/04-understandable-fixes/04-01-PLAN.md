---
phase: 04-understandable-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nextjs-app/shared/components/Label/Label.tsx
  - nextjs-app/shared/components/Label/Label.module.css
  - nextjs-app/shared/components/Label/Label.test.tsx
autonomous: true

must_haves:
  truths:
    - "Screen readers announce 'required' for required form fields"
    - "Visual asterisk is hidden from assistive technology"
    - "Required indicator appears after label text"
  artifacts:
    - path: "nextjs-app/shared/components/Label/Label.tsx"
      provides: "Required field with sr-only text"
      contains: "sr-only"
    - path: "nextjs-app/shared/components/Label/Label.test.tsx"
      provides: "Unit tests for required indicator"
      contains: "screen reader"
  key_links:
    - from: "Label.tsx"
      to: "aria-hidden"
      via: "asterisk span attribute"
      pattern: "aria-hidden.*true"
---

<objective>
Add screen reader text "(required)" to Label component for required fields

Purpose: WCAG 3.3.2 (Labels or Instructions) - required fields must be clearly indicated to screen reader users, not just visually with an asterisk
Output: Label component that announces "required" to screen readers while showing asterisk visually
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-understandable-fixes/04-CONTEXT.md
@.planning/phases/04-understandable-fixes/04-RESEARCH.md
@nextjs-app/shared/components/Label/Label.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Label component with sr-only required text</name>
  <files>
    nextjs-app/shared/components/Label/Label.tsx
    nextjs-app/shared/components/Label/Label.module.css
  </files>
  <action>
Update the Label component to add screen reader text for required fields:

1. In Label.tsx, modify the required indicator rendering:
   - Wrap the asterisk in `<span aria-hidden="true">` to hide from screen readers
   - Add `<span className="sr-only">(required)</span>` after the asterisk
   - The sr-only class should use the existing utility pattern from the project

2. Example structure:
```tsx
{required && (
  <>
    <span aria-hidden="true" className={styles.required}>*</span>
    <span className="sr-only">(required)</span>
  </>
)}
```

3. Ensure the sr-only class follows project conventions. Check if there's an existing sr-only utility class or add one to Label.module.css:
```css
.srOnly {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
```
  </action>
  <verify>
    - `npm run typecheck` passes
    - `npm run lint` passes
    - Inspect Label.tsx to confirm aria-hidden on asterisk and sr-only text present
  </verify>
  <done>
    - Asterisk has aria-hidden="true"
    - Screen reader text "(required)" added with sr-only styling
    - No TypeScript or lint errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for required indicator accessibility</name>
  <files>
    nextjs-app/shared/components/Label/Label.test.tsx
  </files>
  <action>
Add or update tests for the Label component to verify required indicator accessibility:

1. Test that asterisk is hidden from screen readers:
```tsx
it('hides asterisk from screen readers when required', () => {
  render(<Label htmlFor="test" required>Name</Label>);
  const asterisk = screen.getByText('*');
  expect(asterisk).toHaveAttribute('aria-hidden', 'true');
});
```

2. Test that sr-only text is present for screen readers:
```tsx
it('includes screen reader text for required fields', () => {
  render(<Label htmlFor="test" required>Name</Label>);
  const srText = screen.getByText('(required)');
  expect(srText).toBeInTheDocument();
  expect(srText).toHaveClass('srOnly');
});
```

3. Test that non-required labels don't have required indicator:
```tsx
it('does not show required indicator when not required', () => {
  render(<Label htmlFor="test">Name</Label>);
  expect(screen.queryByText('*')).not.toBeInTheDocument();
  expect(screen.queryByText('(required)')).not.toBeInTheDocument();
});
```

If Label.test.tsx doesn't exist, create it following project testing patterns with Vitest and Testing Library.
  </action>
  <verify>
    - `npm test -- Label` passes all tests
    - Tests cover required indicator accessibility
  </verify>
  <done>
    - Unit tests verify aria-hidden on asterisk
    - Unit tests verify sr-only text presence
    - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
# Run tests
npm test -- Label

# Type check
npm run typecheck

# Lint
npm run lint
```
</verification>

<success_criteria>
- Label component renders "(required)" text for screen readers
- Asterisk is hidden from assistive technology via aria-hidden="true"
- All unit tests pass
- UNDR-04 requirement addressed
</success_criteria>

<output>
After completion, create `.planning/phases/04-understandable-fixes/04-01-SUMMARY.md`
</output>
