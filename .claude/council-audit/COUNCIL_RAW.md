# Council raw digest


## client-hiring  —  58/100
**Verdict:** Real senior craft is on display, but the studio doesn't reliably finish or maintain what it ships and it's a one-person shop, so I'd hire for a scoped paid pilot, not bet a production DS or a premium rate on it yet.

**Bottom line:** Conditional hire, not a full bet. The portfolio proves the principal can do elite design-systems work: the foundations layer, the self-auditing contrast story, the Button API and docs are genuinely senior and better than most agencies I'd interview. But three things stop me from signing a production DS contract at a premium rate: (1) the studio repeatedly fails to enforce its own documented rules (react-icons pin) and ships a11y failures its own tooling catches (HCW 1.19:1), so the documentation-to-delivery gap is real; (2) the impressive breadth is ~95% self-declared beta/alpha with a half-resolved shadcn fork, so I'd be paying authority pricing for unfinished inventory; (3) it's a one-person shop with no continuity plan. I'd hire them for a scoped, paid pilot (a real feature or a 10-component hardening sprint) priced as mid-senior, with explicit acceptance gates and a handover clause, then revisit rate and scope once they demonstrate they finish and maintain to my bar, not just theirs.

**Strengths:**
- Self-auditing foundations prove genuine DS competence, not just component output _(EVIDENCE.md Section 8 + screenshot 02-contrast-overview.png: Contrast story computes WCAG ratios per theme and self-flags failures; variables.css ships 4 themes (light/dark/HCB/HCW) plus forced-colors handling at :615-616 (CanvasText))_
- Exemplary component documentation I could hand to my own team _(screenshot 04-button-docs.png + Button.tsx:79-91 JSDoc with live examples; docs cover keyboard, focus, disabled-vs-aria-disabled, reduced motion, forced colors)_
- Button API shows real seniority (orthogonal variant x tone x size x surface) _(Button.tsx:9-25,60-92 discriminated-union button/link polymorphism; dev-mode icon-only-without-name warning at :131-138)_
- Accessibility is wired into tooling, not bolted on _(package.json:265,282,305 (axe-core playwright + jest-axe + storybook a11y addon); 148 .test.tsx with axe; Lighthouse Accessibility 100 (EVIDENCE.md Section 7))_

**Findings:**
- [HIGH] **Studio violates its own written maintenance rule in the live dependency manifest**
  - evidence: package.json:217 "react-icons": "^5.6.0" contradicts CLAUDE.md gotcha ("pinned at 5.5.0; 5.6 drops Adobe SI icons on work pages"). Verified: 15 files import react-icons/si, 12 are Work portfolio pages (Rhythmguard, Tulli, Intrum, SapBuildApps, etc.). Installed 5.6.0 still ships react-icons/si so NOT currently broken, but the floating ^ range means any reinstall can pull a 5.x that breaks their own case-study showcase.
  - so what: Clearest procurement red flag: the team wrote a rule to protect their own portfolio pages and shipped the exact violation. If they don't enforce their own documented guardrails on their own site, I can't trust them to honor my acceptance criteria or change-control under deadline pressure. The gap between what they document and what they ship is real.
  - fix: Pin "react-icons": "5.5.0" exact (no caret) as the rule states, or migrate the 12 work pages to the already-installed simple-icons (package.json:228) and delete the stale gotcha. Reconcile doc and lockfile before any client sees this repo.
- [HIGH] **150 components / 1,159 stories is breadth theater: only 8 are self-declared production-ready**
  - evidence: Contract status across nextjs-app/shared/components/*/*.contract.json: 99 beta, 12 alpha, only 8 stable (Badge, Button, Card, Icon, Label, Link, Text, Title). Barrel nextjs-app/shared/components/index.ts re-exports only 79 of 166 dirs. AGENTS.md still says "80+ components" (doc drift vs ~150).
  - so what: As a buyer weighing breadth as proven track record, the headline inverts on inspection: by the studio's own status field ~95% of the surface is not production-ready and roughly half isn't on the public API. That reads as unfocused, unfinished surface area, not a delivered system, and makes me discount the rate: I'd be paying authority pricing for mostly-beta inventory.
  - fix: Stop counting raw dirs as the proof point. Lead with the 8 stable, fully-documented primitives as evidence of capability, archive or clearly label the alpha/beta sprawl, and align AGENTS.md, the barrel, and any marketing count to the real stable surface.
- [HIGH] **Single-author repo: bus factor of one is the core engagement risk**
  - evidence: git shortlog -sn: 1001 of 1140 commits by one author (Petri Lahdelma); remaining 139 are dependabot[bot]. Zero other human contributors across 1,140 commits.
  - so what: The question procurement must answer: who delivers and supports my system if this one person is unavailable or oversubscribed? A solo studio can produce excellent work (and this is evidence of that), but for a production DS I depend on, single-person continuity is an unmitigated risk that justifies smaller initial scope, a handover/escrow clause, and lower commitment until a second pair of hands is shown.
  - fix: For the engagement: contract a defined bus-factor mitigation (documented handover, named backup, or escrowed source). For the pitch: be upfront it's a solo practice and price/scope accordingly rather than presenting team-scale breadth.
- [HIGH] **A shipped accessibility failure the studio's own tool already flags red**
  - evidence: variables.css:534-536 HCW theme --color-warning:#000 + --color-warning-text:#041b23 (near-black on black); the DS's own Contrast story computes 1.19:1 FAIL (screenshot 02-contrast-overview.png). Shipped unfixed at current HEAD.
  - so what: For a studio selling accessibility as a differentiator, shipping a WCAG failure your own dashboard renders in red is the worst own-goal: the capability to catch it exists and was ignored. A regulated or public-sector buyer (the Tulli/Helsinki work suggests they target these) will find it in five minutes and question whether the a11y story is marketing or practice.
  - fix: Fix the HCW warning pair to clear 4.5:1, and make the Contrast story a failing gate in the release pipeline (release:gate / check:contrast already exist at package.json:102,129) so a red ratio blocks merge.
- [MEDIUM] **Copy-paste structural bug in the flagship token file**
  - evidence: variables.css: ".themeHCW, .themeHCW :root {" opens at :499-500; inside it ".themeHCB .badge" (:510) and ".themeHCB .toast" (:515) sit nested under the HCW selector block.
  - so what: The token file is the artifact a studio points to as proof of rigor; a mis-scoped selector means the HCB badge/toast forced-color-adjust either never applies or applies in the wrong theme. A defect in the showpiece file tells me their review doesn't catch structural errors even in their most-scrutinized code, raising the cost of trusting the rest.
  - fix: Move the .themeHCB .badge / .themeHCB .toast rules out of the .themeHCW block into HCB scope, and add a stylelint/test check that token overrides live under the matching theme selector.
- [MEDIUM] **Two competing component systems tracked in one repo (source-of-truth ambiguity)**
  - evidence: Bespoke nextjs-app/shared/components/ (~150) coexists with a tracked shadcn scaffold: components/ui/ has 12 files (accordion..tooltip) + root components.json (style new-york, iconLibrary lucide, rsc:false). Storybook has ShadcnMigration + MigrationDecisionBoard stories (EVIDENCE.md Section 6).
  - so what: If I adopt this DS, which Button is canonical, @dt/Button or components/ui/button? An unresolved migration in main means I inherit ambiguity and risk of a half-finished re-platform mid-engagement. It signals strategy churn, not a settled authoritative system, and forces me to budget for which-path-won before building on it.
  - fix: Pick one. Either complete the shadcn migration and deprecate the bespoke set, or delete components/ui/ + components.json and the migration stories. Don't ship an unresolved fork in the branch a client evaluates.
- [MEDIUM] **Flagship stable Card is a 40-prop god component with an invalid-HTML a11y trap**
  - evidence: Card.tsx:28-130 exposes ~40 props incl. bodyStyle/headStyle CSSProperties escape hatches (:123-125) and a Legacy body prop (:86). In link mode (:433-442) it wraps innerContent (which can contain Buttons :202-216 and Tabs :191-200) in an <a>, nesting interactive elements in an anchor. Card is status: stable.
  - so what: Card is one of only 8 components certified production-ready, yet it bundles a known invalid-HTML/a11y pattern and a 40-prop surface that's hard to maintain. If their stable bar still admits this, it's softer than mine, and I'd have to re-audit even the components they vouch for, eroding the value of buying a ready system.
  - fix: Split Card into composable subcomponents (Card.Header/Body/Footer/Media) and forbid link mode when interactive children are present (or render the link as an overlay, not a wrapper). Re-gate stable so a11y-invalid patterns can't carry the label.
- [LOW] **Debug/scratch files committed to repo root degrade the first impression**
  - evidence: git ls-files confirms TRACKED: debug-env.js, CLAUDE.md.backup, check_missing_tests.sh, PHASE_2_BREAKING_CHANGES.md, TIER_1_MATERIAL_DESIGN_3_CARD_ANALYSIS.md, claude-desktop-blank-screen-bug-report.md.
  - so what: When I evaluate a studio's repo as a portfolio artifact, the root is the storefront. A .backup file, a bug report, and ad-hoc shell scripts in main signal loose housekeeping, exactly the trait I'm screening for when deciding whether to trust them with my codebase long-term. Cheap to fix, expensive to leave.
  - fix: Remove from tracking (git rm --cached + .gitignore) or relocate keep-worthy notes (PHASE_2_BREAKING_CHANGES, the Card analysis) into docs/. The root should read as intentional.

**Top priorities:**
  1. Reconcile documented rules with shipped reality before any client review: pin react-icons to 5.5.0 (package.json:217) and fix the HCW 1.19:1 warning contrast (variables.css:534-536) — self-inflicted credibility wounds the studio's own docs/tools already flagged.
  1. Reframe and prune the breadth story: lead with the 8 truly-stable, well-documented primitives as proof of capability; archive or relabel the 111 alpha/beta components and fix the 79-of-166 barrel + AGENTS.md count drift so authority survives due diligence.
  1. Resolve structural ambiguity and continuity risk: pick one component system (bespoke vs shadcn scaffold) and address the bus-factor-of-one with a concrete handover/escrow plan, since 1001/1140 commits are from a single author.

## client-clevel  —  61/100
**Verdict:** The "design-systems authority" claim has real substance and a genuine AI-native differentiator, but single-author bus factor plus an unpinned icon dependency that the team's own docs say breaks the portfolio make this "buy the talent now, do not yet bet your org's platform on it."

**Bottom line:** Would I bet my org's design-system platform on this studio today? Not as a turnkey platform, yes as a talent/IP acquisition. The substance is real and rare: enforced lifecycle governance, measured accessibility, and a genuinely differentiated agent-native layer that most "authority" shops only talk about. But the buyer-side risks are concentrated and concrete: a bus factor of one over 16,000 lines of bespoke tooling, an ungated dependency the team itself documented as portfolio-breaking, and an unresolved bespoke-vs-shadcn fork shipped to the public Storybook. The agent-native angle is the strategic asset to pay for; the single-maintainer and dependency-governance gaps are the conditions to put in the contract before I sign. Engage with a knowledge-transfer clause and a 90-day hardening sprint, not a blank-check platform mandate.

**Strengths:**
- The authority positioning is backed by enforced governance, not slideware: a real WIP-to-beta-to-stable lifecycle with machine gates and per-component contracts _(nextjs-app/shared/components/AGENTS.md:79-85 (stable requires a11y snapshots + production consumer + review evidence); contract status counts 99 beta / 12 alpha / 8 stable; release:gate, promote:stable-fleet, parity:verify in package.json:120,129-130)_
- The AI/agent-native angle is a strategic moat, not a distraction: a buyer acquires a quantified, machine-consumable component system aligned with where tooling is heading _(112 scripts in scripts/design-system/ (16,015 LOC); agent-eval harness with golden-intents.json + golden-patterns.json + intent/pattern evals; ds-mcp-stdio.ts MCP server (package.json:144); generated agent-manifest.json)_
- Accessibility is a measured contract, not a checkbox, which materially de-risks regulated, public-sector, and enterprise buyers _(EVIDENCE.md:79-81 + screenshot 02 (Contrast story computes per-theme WCAG ratios and self-flags failures); 148 .test.tsx with axe-core; Lighthouse Accessibility 100 (EVIDENCE.md:73))_
- Dependency hygiene is actively managed, signaling operational maturity for a small shop _(package.json:234-263 (28-line overrides block force-pinning transitive deps: undici, dompurify, path-to-regexp, fast-xml-parser); dependabot is the #2 committer (67 of last 200 commits); security:audit + security:audit:triage scripts present)_

**Findings:**
- [HIGH] **Key-person risk is the dominant due-diligence blocker: bus factor of one across a 16,000-line bespoke platform**
  - evidence: git log -200 --format=%an: 133 of 133 human commits are Petri Lahdelma (the only other author is dependabot[bot], 67); scripts/design-system totals 16,015 LOC of bespoke tooling owned by that one person
  - so what: If this studio loses one individual, the entire design-system platform, its 112 custom scripts, the MCP/agent layer, and the institutional knowledge to operate them go dark overnight. For a CPO/CTO this is the single biggest reason to treat an engagement as a talent/IP acquisition with a knowledge-transfer clause, not as a turnkey platform you can staff against. There is no second maintainer to absorb the handoff.
  - fix: Before any material engagement, contract for (a) a written architecture-decision record and runbook for the 112-script pipeline, (b) a named second maintainer or escrow of the tooling, and (c) a documented onboarding path. Ask the vendor to demonstrate a second person shipping a stable-tier promotion end to end.
- [HIGH] **Unpinned react-icons dependency is a live, ungated breakage risk on the revenue-critical portfolio pages**
  - evidence: package.json:217 react-icons:^5.6.0 resolves to 5.6.0 (node_modules/react-icons/package.json) which CLAUDE.md explicitly flags: "pinned at 5.5.0; 5.6 drops Adobe SI icons on work pages"; 14 files import react-icons/si, 12 of them are Work/*Page.tsx portfolio pages; grep of scripts/, tests/, .github/ found zero version guard or test protecting this
  - so what: The studio's own portfolio is its sales collateral. A floating caret on the exact version their docs say breaks those pages, with no CI or test gate, means the next npm install or dependabot bump can silently break the work showcase that closes deals. This is a governance-maturity tell: the team documented the landmine but did not pin it or gate it. For a buyer, it undermines confidence that "authority" discipline extends to their own dependency lifecycle.
  - fix: Pin react-icons to exactly 5.5.0 in package.json (remove the caret), or migrate the 14 si imports to the already-installed simple-icons (package.json:228) / @phosphor-icons (line 167) and drop react-icons. Add a smoke test or a deps:report assertion that fails the build if react-icons resolves above 5.5.0 while react-icons/si is imported.
- [HIGH] **Source-of-truth ambiguity: a bespoke system and a tracked shadcn scaffold coexist with an unresolved, in-repo "migration decision" still open**
  - evidence: root components.json (shadcn, style new-york) + components/ui/{12 primitives}; nextjs-app/shared/stories/MigrationDecisionBoard/ ships 5 comparison stories (Button/Dialog/Form/ContactFlow) using legacy/wrong/proposed/defer verdict states; only 5 non-story files import @/components/ui and 3 of those are infra (check-drift.ts, executors.ts, index re-exports)
  - so what: A buyer scaling this across an org needs ONE answer to "which component do I use." Shipping a documented but unfinished migration board signals the team is mid-decision on its own foundation. The risk is not the shadcn code itself (it is nearly inert, 5 thin consumers) but the strategic ambiguity: are we adopting a bespoke CSS-Modules system or a Radix/Tailwind one? That fork doubles onboarding cost and invites teams to diverge. It is the kind of thing that looks like indecision in vendor due diligence.
  - fix: Make and document the call: either delete components/ui + components.json and the migration stories (commit to bespoke), or publish a dated ADR with a migration timeline and freeze new bespoke primitives. Do not ship a 5-board "decision in progress" to a Storybook a buyer will see.
- [MEDIUM] **Lifecycle maturity is inverted for an "authority": only 8 of ~120 contracted components are stable; 99 are beta**
  - evidence: contract status counts: 99 beta, 12 alpha, 8 stable (Card, Title, Label, Button, Link, Text, Icon, Badge); AGENTS.md:79-85 defines stable as the only production-ready tier
  - so what: The governance model is genuinely good, which makes the distribution damning by its own standard: 93% of the catalog is self-classified as not-yet-production-ready. A buyer reading the contracts at face value sees a system where almost nothing has cleared the studio's own production bar. Either the bar is aspirational theater or the system is far earlier than the "authority" positioning implies. Both readings raise TCO: you inherit the promotion backlog.
  - fix: Publish a realistic promotion roadmap (how many components reach stable per quarter, and the gating cost per component). If beta is in fact production-safe in practice, rename the tiers so the public-facing maturity signal matches reality; do not let 99 beta entries undercut the authority claim.
- [MEDIUM] **Total cost of ownership is inflated by three coexisting styling systems and a hybrid Next.js + legacy Vite monorepo**
  - evidence: package.json carries CSS-Modules (repo standard), Tailwind v4 + class-variance-authority (lines 189,321) AND styled-components ^6.4.2 (line 229); styled-components is imported in only 1 nextjs-app file; CLAUDE.md identifies the repo as "Next.js 16 production + Vite legacy" hybrid with api-legacy-vercel-functions/
  - so what: Every styling system is a dependency to patch, a convention to teach, and a way for code to drift. styled-components at 1 consumer is pure carrying cost (an extra runtime CSS-in-JS dep for almost no usage). The Vite legacy surface is a second build system to keep alive. For a CTO costing a multi-year engagement, this is real ongoing maintenance overhead that does not show up in a demo.
  - fix: Remove styled-components and migrate its single consumer to CSS Modules or Tailwind. Publish a dated sunset plan for the legacy Vite app and api-legacy-vercel-functions so the maintenance surface is converging, not permanent.
- [MEDIUM] **Shipped a11y defect in a high-contrast theme contradicts the accessibility selling point**
  - evidence: variables.css:534-536 HCW theme --color-warning:#000 on --color-warning-text:#041b23; the DS's own Contrast story computes 1.19:1 FAIL (EVIDENCE.md:47, screenshot 02); also mis-nested .themeHCB rules sitting inside the .themeHCW block (variables.css:510-518)
  - so what: Accessibility is positioned as a differentiator and is the reason a regulated buyer would pay a premium. A self-detected 1.19:1 contrast failure shipped to main means the studio's own automated gate flagged it and it went out anyway. That is a process gap (the gate exists but does not block), which for a buyer is more concerning than the single pixel: it means the safety net is advisory, not enforcing. The copy-paste structural bug in the theme block reinforces that the token layer is hand-maintained and under-tested.
  - fix: Make the Contrast story a blocking CI gate (fail build on any pair below WCAG AA), fix the HCW warning pair, and move the mis-nested .themeHCB rules into the correct block. Then cite "zero failing contrast pairs, CI-enforced" as the actual selling point.
- [LOW] **Repo hygiene leaks internal artifacts that erode buyer confidence in a technical audit**
  - evidence: git ls-files shows tracked root clutter: CLAUDE.md.backup, PHASE_2_BREAKING_CHANGES.md, TIER_1_MATERIAL_DESIGN_3_CARD_ANALYSIS.md, check_missing_tests.sh, debug-env.js, claude-desktop-blank-screen-bug-report.md
  - so what: In a code-level due-diligence (which any serious enterprise buyer runs), a clean root signals discipline and a cluttered one signals the opposite. These files are harmless individually but collectively read as "work-in-progress committed to main," which is exactly the impression an authority-positioned vendor cannot afford when the buyer is deciding whether the polish is real or demo-deep.
  - fix: Move analysis/debug artifacts to a docs/ or scratch/ dir excluded from the tracked surface, or delete them. A buyer's first git clone should look like a product, not a workspace.

**Top priorities:**
  1. Resolve key-person risk: contract a knowledge-transfer/runbook + named second maintainer (or tooling escrow) before any platform-scale engagement; 133/133 human commits are one person.
  1. Pin react-icons to 5.5.0 (or migrate off /si) and add a CI gate; the floating ^5.6.0 is the exact version the team's own docs say breaks 12 portfolio pages, and nothing protects it.
  1. Kill the source-of-truth ambiguity: publish a dated ADR either deleting the shadcn scaffold + migration boards or committing to the migration, so an adopting org has one component answer.

## design-lead-peer  —  58/100
**Verdict:** A genuinely elite documentation and a11y-tooling layer sitting on an incoherent foundation: a type scale whose API advertises 7 steps the tokens don't back, a greyscale ramp and radius scale with no monotonic order or lint enforcement, semantic tokens that invert their own names, and a 40-prop Card that nests interactive elements in an anchor. The craft is real but the primitives betray the "authority" positioning.

**Bottom line:** I would not approve this for the "design-systems authority" shelf in its current state, but it is closer than most. The documentation, the orthogonal Button model, and the self-computing Contrast/forced-colors tooling are genuinely above the bar - I would happily show a client the Button docs page. What sinks it is the foundation: a type scale whose API lies about which steps exist, a greyscale and radius system with no order and no enforcement, dark-mode tokens that flatten heading/body/link into one blue, and a Card that violates HTML semantics it claims to uphold. These are not polish items; they are the primitives the whole system is built on, and a peer reviewer will judge the system by them, not by the (excellent) docs wrapped around them. Fix the type scale, the dark-mode roles, and turn the contrast gate from advisory to enforcing, and this jumps from "promising" to "credible authority."

**Strengths:**
- Component documentation is best-in-class and worth protecting as the system's signature _(screenshot 04-button-docs.png: In use / How to use (live code) / When (not) to use / Accessibility (keyboard, focus, disabled-vs-aria-disabled, reduced motion, forced colors) / dated promotion notes; Button.tsx:123-139 ships a dev-only icon-only-without-accessible-name console.warn)_
- Self-auditing Contrast foundation computes live WCAG ratios per theme and visibly flags its own failures _(screenshot 02-contrast-overview.png renders 'Warning text on warning' at 1.19 Fail in red for HCW; my own math confirms ratio(#041b23,#000)=1.19:1)_
- Button API is a textbook orthogonal model _(Button.tsx:8-19,92 - variant (weight) x tone (semantic color) x size x surface as independent axes, discriminated-union button/link polymorphism (ButtonAsButton | ButtonAsLink), tone auto-maps to status icon (TONE_TO_STATUS:20-25))_
- Serious multi-theme + forced-colors + prefers-contrast coverage at the token layer _(variables.css:322-661 - light/dark/HCB/HCW plus @media (forced-colors: active) mapping to system colors (Canvas/CanvasText/LinkText) and @media (prefers-contrast: more) including a dark-mode variant)_

**Findings:**
- [HIGH] **Greyscale has no coherent ramp and radius has no scale; only color is lint-enforced, so radius discipline is on the honor system and has decayed**
  - evidence: Greys are non-monotonic: gray #5e5e5e=6.48:1 is DARKER than gray-medium #666=5.74:1, and muted #6c757d=4.69:1 is lighter than both (variables.css:109-118, my contrast run). Radii: only --radius-sm:2px/-md:0.25rem/-lg:8px exist (variables.css:178-180), mixed units. Raw border-radius literals across modules spell pills 4 ways (999px x10, 9999px x3, 100px x3, 100rem x2) plus 13+ distinct values. .stylelintrc.json:102-110 enforces strict-value for /color/,fill,stroke ONLY - radius/spacing ungated.
  - so what: Naming should encode order: a designer cannot reason about 'gray vs gray-medium vs muted' when the values do not sort by their names. A radius scale missing xl/full forces every author to hardcode pills, yielding four spellings of 'round'. The asymmetry is the tell - color is disciplined because a linter enforces it, radius is chaos because nothing does. An authority system enforces its whole scale, not just the easy axis.
  - fix: Rebuild the grey ramp as an ordered numeric scale (--color-gray-50..900) with monotonic luminance, retire the ambiguous gray/gray-medium/muted aliases behind it. Add --radius-xs/sm/md/lg/xl/full in ONE unit consistently, define --radius-full:9999px once, and extend scale-unlimited/declaration-strict-value to cover border-radius (ideally padding/margin/gap). Codemod raw literals to tokens.
- [HIGH] **Dark mode is 'everything blue': title, primary, and body-copy tokens all collapse to one saturated blue, and literal-named tokens invert**
  - evidence: variables.css dark block: --color-title:#6fa8ff (:329), --primary-text-color:#6fa8ff (:331), --main-body-copy-color:#6fa8ff (:332), --color-primary:#6fa8ff (:336) - four roles, one blue. screenshot 03-type-scale.png: every heading specimen AND section labels render blue. screenshot 01: the LIGHT-column 'Primary' swatch also renders blue, not production #041b23. Plus inversion: dark sets --color-white:#181a1b (:356) and --color-black:#fff (:357).
  - so what: Body copy is not supposed to be a saturated accent - #6fa8ff as --main-body-copy-color makes paragraphs vibrate and read as link text, eroding the heading/body/link distinction typography exists to create. Tokens literally named --color-white that resolve to near-black are a trap: anyone reaching for 'white' gets the opposite in dark mode. This is a foundation defect a peer studio would lead their critique with.
  - fix: Separate roles in dark: keep an accent blue for --color-primary/links but set --color-title and --main-body-copy-color to near-neutral ink (e.g. #e8eaed / #c9ccd1). Rename --color-white/--color-black to role tokens (--color-surface-max / --color-ink-max) or freeze them as true literals with separate inverting tokens. Fix the Storybook Color/Contrast specimens to isolate each theme's :root so the light swatch shows #041b23.
- [HIGH] **Card is a 40-prop god component that nests interactive elements inside an anchor (invalid HTML + a11y) and ships two overlapping border systems**
  - evidence: Card.tsx:28-130 - ~40 props spanning Title, Text, Tabs, Badge, action Buttons, statusMessage(role=alert), loading skeleton, cover media. link mode (:433-442) wraps innerContent - which includes footer action <Button>s (:202-216) and <Tabs> (:191-200) - in a single <Link>/<a>. Two border systems coexist: bordered default true (:146) AND variant default 'outlined' (:148), with .bordered/.unbordered (Card.module.css:437,441) layered on .outlined/.filled/.elevated. Carries a legacy body prop (:85) plus bodyStyle/headStyle CSSProperties escape hatches (:123-125).
  - so what: Nesting <button>/tablist inside <a> is invalid HTML and breaks keyboard/AT semantics - the very accessibility this system markets. A 40-prop surface with config-object passthroughs is un-reviewable, and two props (bordered + variant) independently control the border, so 'outlined + bordered=false' is a contradictory state the types permit. This is API sprawl a design lead would block at review.
  - fix: Split into Card + Card.Header/Body/Media/Footer compound parts, or a separate LinkCard that forbids actions/tabs at the type level via a discriminated union mirroring ButtonAsButton|ButtonAsLink. Collapse bordered into variant (outlined implies a border; drop the bordered prop). Remove bodyStyle/headStyle and the legacy body prop or deprecate with a removal date.
- [HIGH] **The Typography scale is broken in production: the size API advertises 7 steps but the tokens back only ~4, with xs===s**
  - evidence: Title.module.css:25-29,49 references --font-size-title-xxs/-xs/-xxl but variables.css defines only -s/-m/-l/-xl (grep: xxs/xs/xxl NONE DEFINED), so <Title size='xxs|xs|xxl'> falls back to the 3.75rem base .title rule. Text.module.css:18 AND :22 both resolve to --font-size-text-s (xs===s), and --font-size-text-xxs/-xl/-xxl are undefined. Title.tsx:5 exposes xxs..xxl while Card.tsx:34 narrows titleProps.size to s|m|l|xl - primitive and consumer scales diverge at the type level.
  - so what: A design-systems authority's first artifact is a working type scale. Here the public API promises seven steps; three silently collapse to the largest heading and a fourth is a duplicate. Consumers picking size='xs' or 'xxl' get visually wrong output with no error. The screenshot 03 specimen even omits exactly these steps, so the docs structurally hide the defect rather than expose it.
  - fix: Define the full --font-size-title-* and --font-size-text-* clamp ladder (xxs..xxl) in variables.css with distinct values, or shrink the TitleSize/TextSize unions to the steps that actually exist. Generate the Typography specimen from the token source so missing steps render visibly broken. Export TitleSize/TextSize from the primitives and reuse in Card instead of re-declaring narrowed literals.
- [HIGH] **The system computes a 1.19:1 contrast failure in its own Contrast story and ships it; the gate audits only 7 pairs; HCB overrides are misnested under HCW**
  - evidence: variables.css HCW: --color-warning:#000 (:534) with --color-warning-text:#041b23 (:536) -> near-black on black; screenshot 02 shows 1.19 Fail. Structural copy-paste bug: .themeHCB .badge and .themeHCB .toast (:510-518) are nested INSIDE the .themeHCW selector, so those HCB forced-color overrides never apply. screenshot 02 footer: 'Pairs audited: 7'.
  - so what: Detecting a failure then shipping it is worse than not detecting it - it proves the gate is advisory, not enforcing. A high-contrast theme that fails contrast is self-negating. A 7-pair audit cannot cover ~150 components, so green checkmarks elsewhere give false confidence. The misnested HCB rules mean a whole theme's overrides are silently dead.
  - fix: Fix HCW warning to a passing pair (light warning surface, --color-warning-text:#000 -> 21:1). Move .themeHCB .badge/.toast out of the .themeHCW block to top level. Wire the Contrast computation into CI as a FAILING gate covering every semantic fg/bg pair across all four themes, not 7 hand-picked pairs.
- [MEDIUM] **Disabled states do not track the color they derive from, and a duplicate focus-ring token invites the exact regression its comment documents**
  - evidence: Light --color-primary:#041b23 (dark teal, :126) but --color-primary-disabled:#0000ff50 (pure blue @50%, :127) -> my blend gives #8080ff, a different hue family. Dual focus tokens --color-focus-ring (:103) and --focus-ring-color (:167) both = var(--color-primary); the comment at :99-102 admits the first was a past invisible-focus bug patched by adding a SECOND parallel token rather than consolidating.
  - so what: A disabled primary that is blue when primary is teal looks like a bug to any designer eyeballing states side by side - disabled should read as a desaturated version of the same hue. Two tokens for one concept (focus ring) guarantees future drift: someone updates one and not the other, reintroducing the invisible-focus regression the comment itself describes.
  - fix: Derive --color-primary-disabled from --color-primary: color-mix(in srgb, var(--color-primary) 38%, transparent), so it tracks per theme. Collapse --color-focus-ring and --focus-ring-color into one token and codemod all references to it.
- [MEDIUM] **Disabled link path is a usability trap: aria-disabled is set but the link still navigates**
  - evidence: Button.tsx:226-240 - when isLink and disabled, the <a> gets aria-disabled but keeps its href, has no onClick/keydown suppression, and no tabIndex=-1. A keyboard or pointer user can still activate a 'disabled' link. The component's own Accessibility docs (screenshot 04) explicitly discuss disabled-vs-aria-disabled, so the gap contradicts its stated contract.
  - so what: 'Disabled' that still works is the classic accessibility lie - sighted users see a dimmed control and assume it is inert, AT announces 'dimmed', but activation navigates anyway. On a system that documents this exact distinction as a selling point, the implementation undercutting the doc is a direct credibility hit.
  - fix: In the disabled link branch, drop href (render as non-navigable), set tabIndex={-1}, and add onClick preventDefault/stopPropagation. Better: when disabled, render <button disabled> or <span role='link' aria-disabled> with no href, matching the documented behavior.
- [LOW] **Foundations docs and contract claims drift from reality, and specimens are structurally incapable of surfacing token gaps**
  - evidence: AGENTS.md:11 claims '80+ UI components' (actual ~150, evidence pack §2). Foundations captioned '164 tokens in the light-theme :root block' (screenshots 01/03) yet the type-scale specimen omits exactly the Title XXS/XS/XXL and Body XXS/XS/XL/XXL steps whose tokens are missing. 8-file-per-component contract met by only ~120/216 tsx (evidence pack §2).
  - so what: A design system's docs are a contract with consumers. When the foundation specimen quietly renders only the steps that happen to work, reviewers cannot see the broken ones - the documentation cannot surface its own type-scale defect. Drifted counts ('80+', '164 tokens') read as un-maintained to a peer evaluating rigor.
  - fix: Generate Typography/Color specimens from the token source so every declared scale step renders (missing tokens then show as visibly broken). Auto-derive component and token counts in docs from the manifest instead of hardcoding. Reconcile AGENTS.md identity copy with the manifest in the validate:agent-docs gate.

**Top priorities:**
  1. Fix the type-scale token integrity: define the full --font-size-title-*/--font-size-text-* ladder (or shrink the TitleSize/TextSize unions to real steps), eliminate xs===s, and reconcile Title vs Card size APIs - a broken type scale is the most damaging single defect for the 'authority' claim
  1. Fix dark-mode color roles so title and body copy are neutral ink rather than the same saturated blue as primary/links, and de-invert the literal-named --color-white/--color-black tokens
  1. Make the existing self-auditing Contrast gate enforcing in CI across all four themes (fix the 1.19:1 HCW warning and the HCB-under-HCW misnesting first), and extend stylelint strict-value beyond color to radius/spacing so the grey and radius scales get rebuilt as ordered, lint-enforced ramps

## junior-designer  —  58/100
**Verdict:** The motion language and Button craft genuinely sparkle and the agent-native/forced-colors ambition is exciting, but the type scale is one flat weight and the dark theme paints every heading, body, and primary the same periwinkle blue: the foundations look unfinished in the exact stories meant to sell the system.

**Bottom line:** Would I want to work here / build on this? The motion vocabulary, the self-auditing Contrast story, the forced-colors stories, and the Button's press-and-reduced-motion craft genuinely excite me, that's a team with taste and an agent-native ambition I'd want in on. But right now the surfaces a designer judges first, the type scale and the dark theme, look unfinished: one flat weight and a monochrome-blue dark mode where heading, body, and primary are literally the same color, all under floating WIP badges. The bones are inspiring; the foundations are dressed in a draft. Fix the dark hierarchy, the weight axis, and ship a shadow ramp and this jumps from competent-but-dated to a system designers brag about.

**Strengths:**
- Motion is a real language, not an afterthought. Six named easing curves with intent comments (ease-out-expo for entrances, ease-spring overshoot for playful), six durations, stagger + distance tokens. This is the single most inspiring layer in the system. _(nextjs-app/shared/styles/variables.css:221-283 (full motion token block); Motion.stories.tsx exists at nextjs-app/shared/foundations/stories/Motion.stories.tsx)_
- Button micro-interactions are the kind of detail that makes a designer want to work here. One --btn-accent var drives variant x tone, instant scale(0.97) press feedback, and a reduced-motion branch that strips transform/filter but keeps color transitions instead of killing all motion. _(nextjs-app/shared/components/Button/Button.module.css:21-39 (transition + press), :55-71 (nuanced reduced-motion))_
- The Contrast story is a flex worth bragging about: it computes live WCAG ratios per theme and visibly fails its own HCW warning row at 1.19. A foundations page that polices itself is rare and exciting. _(screenshot 02-contrast-overview.png (HCW 'Warning text on warning' renders 1.19 Fail in red); EVIDENCE.md:47)_
- Fluid clamp() type and a vibrant accent palette (pink #f205c5, purple #812eff, cyan #71efff, violet #bc6dff) give the system genuine modern-studio potential. _(variables.css:26-56 (clamp scale), variables.css:88-93 (accent palette))_

**Findings:**
- [HIGH] **The type-scale story renders as one undifferentiated weight, so the proudest foundations page looks flat and generic**
  - evidence: screenshot 03-type-scale.png (Display XL down to Button M all read as the same weight/feel); variables.css:64 only defines --font-weight-title:700 and --font-weight-text:400; Title.module.css:8 hardcodes font-weight:500 (ignoring the 700 token); no weight axis on Title.tsx (grep for weight/700/bold returned nothing)
  - so what: A type scale is where a junior designer first judges craft. With no contrast between weights, no tracking shown per step, and the heading component silently overriding its own weight token (500 vs 700), the most-screenshotted page in the system reads as competent-but-dated rather than inspiring. It undersells fonts as expressive as Syne + Satoshi.
  - fix: Add a weight axis (--font-weight-light/regular/medium/semibold/bold) and a tone/weight prop on Title; reconcile Title.module.css:8 to use var(--font-weight-title); render tracking + weight + line-height per step in the story so the scale shows rhythm, not just size.
- [HIGH] **Dark theme collapses three semantic roles into the same periwinkle blue, so every heading, body-copy token, and primary look identical**
  - evidence: variables.css:329 --color-title:#6fa8ff, :331 --primary-text-color:#6fa8ff, :332 --main-body-copy-color:#6fa8ff, :336 --color-primary:#6fa8ff; visible in screenshot 03 (every heading blue) and screenshot 01 (dark Primary swatch is the same blue as the heading)
  - so what: In dark mode there is zero visual hierarchy between a heading, body copy, and an interactive accent: they are byte-for-byte the same hue. To fresh eyes this reads as a theming bug, not a design choice, and it makes the whole dark palette feel monochrome-blue and heavy. Dark mode is table stakes for modern taste and this is the first thing a designer would screenshot as 'broken'.
  - fix: Give dark headings a near-white ink (e.g. #f5f7ff), reserve the blue strictly for --color-primary/interactive, and desaturate body copy to a neutral grey. Body copy should never be a saturated accent (--main-body-copy-color:#6fa8ff).
- [HIGH] **No elevation/shadow token scale despite shipping an Elevation foundations story: depth has no design language**
  - evidence: grep for shadow/elevation in variables.css returns only --modal-shadow (:173) and a forced-colors override (:612); yet Elevation.stories.tsx and tokens/production/elevation.json exist at nextjs-app/shared/foundations/
  - so what: Modern interfaces (Linear, Stripe, Raycast) lean on layered, tinted shadows for hierarchy and that 'crafted' feel. A single 0 4px 8px rgb(0 0 0 /20%) modal shadow means cards, popovers, toasts, and modals can't share a coherent depth ramp. Publishing an Elevation story with effectively no elevation tokens is exactly the trying-too-hard-but-thin signal that erodes authority.
  - fix: Define a 4-6 step shadow ramp (--shadow-xs through --shadow-xl) with subtle color-tinted shadows, wire them into Card/Modal/Toast/Tooltip, and make Elevation.stories.tsx render the ramp.
- [MEDIUM] **Radius scale is 3 mixed-unit steps with no pill/full token, so rounded UI feels inconsistent and slightly dated**
  - evidence: variables.css:178-180 --radius-sm:2px, --radius-md:0.25rem, --radius-lg:8px (px vs rem mixed); no --radius-full despite avatars (DonnyAvatar) and pill badges existing
  - so what: A radius scale jumping 2px to 8px with no full/pill option forces components to hardcode 9999px for avatars and chips, which is how rounding drifts and the system starts to look hand-assembled. Consistent, generous radii are a big part of why current tools feel friendly and modern.
  - fix: Normalize to one unit (rem), add --radius-xs/sm/md/lg/xl and --radius-full:9999px, and audit avatar/badge/chip to consume --radius-full.
- [MEDIUM] **The vibrant accent palette barely touches the component layer, so the brand's most exciting colors never show up in the system**
  - evidence: variables.css:88-93 defines 6 accents; grep across all shared component modules finds only 11 usages, almost all on bespoke pages (Home.module.css:216-241, AboutPage.module.css:18) rather than DS primitives
  - so what: The pink/purple/cyan/violet palette is the freshest, most designer-attractive asset in the whole system, and it's effectively decorative wallpaper confined to a couple of marketing pages. The reusable components (the actual product) render in dark teal + greys, so the system feels more corporate and generic than the brand actually is.
  - fix: Promote accents into the semantic layer (data-viz series, tag/badge color sets, focus/selection accents, illustration tokens) and show them in a Storybook 'Expressive color' story so designers see they're usable, not just brand chrome.
- [MEDIUM] **Foundations stories ship visibly broken in Storybook (alpha badges, mis-scoped HCB overrides), which undercuts the 'authority' first impression**
  - evidence: screenshot 01 + 03 show floating 'Work in progress (alpha)' badges over the Theme matrix and Display row; variables.css:510-518 places .themeHCB .badge/.toast inside the .themeHCW block (EVIDENCE.md:48 confirms HCB overrides scoped under HCW)
  - so what: A junior designer evaluating whether to adopt or join judges the polish of the docs surface itself. WIP badges stamped across the headline color and type stories, plus a copy-paste theming bug that means HCB badge/toast styling silently never applies, signal 'in flux' exactly where the system should feel most finished.
  - fix: Gate or remove WIP badges on foundations once a11y/visual/translation are verified (per CLAUDE.md rule), and move the .themeHCB .badge/.toast rules out of the .themeHCW block into a real .themeHCB scope.
- [LOW] **Two dual-token pairs (focus + the literal-named color tokens) make the token catalog feel patched rather than authored**
  - evidence: variables.css:103 --color-focus-ring AND :167 --focus-ring-color both = var(--color-primary) (the :99-102 comment admits the first was a past invisible-focus bug patched by adding a parallel token); :356-357 dark sets --color-white:#181a1b and --color-black:#fff
  - so what: To fresh eyes browsing the Token catalog, two tokens for the same focus ring and a --color-white that is actually dark grey read as confusing and slightly amateur. Token naming is the system's vocabulary; inverted literals and duplicate concepts make designers distrust it and hardcode instead.
  - fix: Consolidate to a single --focus-ring-color and codemod references; rename literal --color-white/--color-black to role tokens like --color-surface-base / --color-ink so the value can invert without the name lying.

**Top priorities:**
  1. Fix the dark theme hierarchy: stop painting headings, body copy, and primary the same #6fa8ff (variables.css:329-336). Distinct heading ink, neutral body, accent reserved for interactive. This is the most visible 'looks broken' issue.
  1. Give the type scale a real weight axis and reconcile Title's hardcoded 500 vs the 700 token, then show weight/tracking/line-height per step so screenshot 03 reads as crafted rhythm, not flat sizes.
  1. Ship an actual elevation/shadow ramp (4-6 tinted steps) wired into Card/Modal/Toast and promote the vibrant accent palette into the component layer so the system looks as modern as the brand and the Elevation story stops being empty.

## ux-researcher  —  52/100
**Verdict:** Foundations and docs are authority-grade, but the form-field layer, the part real users and developers touch most, is fragmented into 4+ incompatible families with broken label/error wiring, inconsistent status semantics, and a disabled-link trap; usability behavior is not consistent across the system.

**Bottom line:** As a UX researcher I would not ship this as the reference design system it claims to be: the foundations, contrast self-audit, and Button docs are top-tier, but the layer real users and developers touch most, form fields and interactive states, is internally inconsistent and contains verified accessibility breakages (FormField never wires errors/help to the control, disabled links navigate, error banners announce politely, the required marker doubles as an error marker). Critically, there is no evidence of real user validation: defects this basic survive because confidence rests on a 7-pair contrast gate and component docs, not on observed task completion. Pick one field system, make the documented aria contracts real and tested, fix the affordance traps, then earn the authority label."

**Strengths:**
- Button documentation models the right usability rigor (keyboard, disabled-vs-aria-disabled, reduced motion, forced colors, when-NOT-to-use) _(screenshots/04-button-docs.png — explicit Accessibility section + When to use / When not to use; mirrored by dev-only icon-only-without-name warning in Button.tsx:131-138)_
- The Contrast foundations story computes WCAG ratios per theme and self-flags its own failures instead of asserting compliance _(screenshots/02-contrast-overview.png — HCW 'Warning text on warning' shown as 1.19 Fail in the team's own harness)_
- TextArea is the one form control that wires assistive-tech associations correctly and composably _(TextArea.tsx:69-76 builds aria-describedby from helper+error+count+inherited, and TextArea.tsx:89 sets aria-invalid from error — the pattern every sibling should copy)_

**Findings:**
- [BLOCKER] **FormField generates error/helper IDs but never connects them to the control — the documented aria-describedby wiring does not exist**
  - evidence: FormField.tsx:30-31 creates helperId/errorId; :40 renders {children} verbatim; :43-54 puts the IDs only on <p> elements; grep confirms NO cloneElement/Children.map in FormField. Yet FormField.stories.tsx:37 documents helperText as 'Hint text wired via aria-describedby' and the Default story (:10) ships <FormField helperText><TextInput/></FormField>.
  - so what: A screen-reader or voice user who focuses the field hears the label only — never the help text and, critically, never the validation error. The component makes a written accessibility promise it does not keep, on a 'beta'-promoted molecule, so every developer who trusts the contract ships an inaccessible form. CheckboxField.tsx:45-53 has the same gap (sets aria-invalid but the error <p> is not referenced by the input's aria-describedby).
  - fix: Wire the IDs into the single child: React.cloneElement(children, { id, 'aria-invalid': !!error || undefined, 'aria-describedby': [error?errorId:null, helperText&&!error?helperId:null, children.props['aria-describedby']].filter(Boolean).join(' ')||undefined }). Add a Storybook play/axe assertion that the rendered input actually carries aria-describedby pointing at the helper, so the contract can't silently regress again.
- [BLOCKER] **Four-plus competing input/field families with mutually incompatible error contracts; production form bypasses the design-system molecule entirely**
  - evidence: error prop types diverge: TextInput.tsx:14 `error?: boolean` vs Select.tsx:27 / FormField.tsx:10 / CheckboxField.tsx:15 / Combobox.tsx:33 `error?: string` vs TextArea.tsx:12 `error?: string | boolean`. Separate systems coexist: nextjs-app/.../TextInput (Tailwind/shadcn-style), Inputs/TextArea, Select (CSS Modules), plus tracked root components/ui/{input,select,textarea,checkbox,label}.tsx. ContactForm.tsx:8 imports `@dt/Inputs/TextArea` and :401 uses Select directly — it does not use FormField at all.
  - so what: There is no single 'how do I build a form field' answer. A developer who learns `error="message"` on Select then writes `error="message"` on TextInput silently gets a truthy-string that styles an error with no message shown, or passes a boolean where a string is expected. Inconsistent affordances mean inconsistent error UX for end users across the very flows (contact, newsletter) that convert. That the team's own production form skips FormField is the strongest signal the molecule isn't trustworthy.
  - fix: Pick ONE field architecture (the TextArea pattern is the soundest), normalize `error: string` everywhere (drop boolean-only), make TextInput/Select/Checkbox compose under FormField via cloneElement, and delete or quarantine the root components/ui shadcn duplicates and the legacy Inputs/* family behind a documented deprecation. Refactor ContactForm onto the canonical path as the proof-of-use required for 'stable'.
- [HIGH] **Disabled Button-as-link still navigates and is keyboard-focusable — users click something that looks disabled and the app moves**
  - evidence: Button.tsx:226-240 — the anchor branch keeps `href`, sets only `aria-disabled={disabled||loading}`, adds NO onClick/onKeyDown suppression and NO tabIndex={-1}. The button branch (:247) correctly uses native `disabled`. Button docs (screenshots/04-button-docs.png, Accessibility) claim aria-disabled 'prevents nav'.
  - so what: A greyed-out 'Submit'/'Continue' rendered via <Button href> is fully clickable and Tab-focusable; a real user (especially keyboard/AT) activates a control the UI signals as inert and is navigated away mid-task or while loading — a classic trust-breaking affordance mismatch. The documentation actively asserts the opposite of the behavior, so reviewers won't catch it.
  - fix: In the link branch, when disabled||loading: drop href (or set aria-disabled + onClick={e=>e.preventDefault()} + onKeyDown guard for Enter), set tabIndex={-1}, and add role-appropriate handling. Add a test that a disabled link does not fire navigation on click and is removed from the tab order, matching the doc.
- [HIGH] **Inputs/TextArea overloads the required asterisk to mean 'error' and uses the label text as the element id**
  - evidence: Inputs/TextArea.tsx:112 `required={!!error}` and :113 `tooltipText={error}` on the Label; :111 `htmlFor={label}` and :120 `id={label}`.
  - so what: Two distinct usability defects in one control: (1) the '*' required marker appears only when the field is invalid, so the universal 'required' signal now means 'error' — users and AT (Label.tsx:43-47 also emits sr-only '(required)') are told the field is required only after they get it wrong, which is incoherent. (2) Using the human label as id means any label with spaces yields an invalid id and any two same-labeled fields collide, breaking click-to-focus and label association exactly where forms repeat (e.g. multiple 'Email'/'Message').
  - fix: Generate the id with useId() (as Select/TextArea-canonical already do) and bind htmlFor to it; never derive id from label text. Drive the required marker from an actual `required` prop, and surface errors through HelperText state='error' + aria-describedby, not by repurposing the required affordance.
- [HIGH] **Status/alert semantics are inconsistent across the system — AlertBanner announces errors as polite status and hardcodes English UI copy**
  - evidence: AlertBanner.tsx:38-43 always sets role="status" + aria-live="polite" even for tone="error"; :57-64 hardcodes 'Close' and aria-label="Dismiss alert". Contrast: HelperText.tsx:64, Card.tsx:346, Toast.tsx:76, FormField.tsx:46 all correctly switch to role="alert" on error.
  - so what: An error AlertBanner that appears after an action is announced politely (queued, often after focus moves) instead of assertively, so AT users may miss a failure message — while the rest of the DS does the opposite, so behavior depends on which component the developer happened to pick. The hardcoded English also violates the repo's i18n MUST (EN/FI/SV), so Finnish/Swedish users see an English 'Close' inside an otherwise localized alert.
  - fix: In AlertBanner, set role and aria-live from tone (error -> role='alert'/assertive; else status/polite) to match HelperText/Card/Toast, and route the dismiss label + button text through i18n keys. Codify the role/aria-live mapping once in a shared util so every status surface is consistent.
- [MEDIUM] **Card's ~40-prop API plus interactive/link/onClick modes invite inconsistent, invalid interactive nesting**
  - evidence: Card.tsx:28-130 (CardProps ~40 fields incl. titleProps/subTitleProps/bodyProps/iconProps/badgeProps/statusMessageProps and legacy `body` at :85-86); :433-442 wraps innerContent in <Link> while innerContent (:402-411) can contain action Buttons (:202-216) and Tabs (:191-200) — interactive controls inside an anchor. Three overlapping activation modes: link (:433), interactive/onClick div with role=button (:444-451), and footer action Buttons.
  - so what: When one component can be a static surface, a button, a link, AND can host its own buttons/tabs, every team wires it differently and some combinations are invalid (a Tab or Button inside an <a> is non-operable/ambiguous for keyboard and AT — nested interactive). The prop surface is too large to learn, so usage drifts and accessibility regressions hide in 'valid' prop combos.
  - fix: Split into Card (presentational) + LinkCard/ClickableCard (single activation target, no nested interactive children allowed) and have link mode refuse/guard actions+tabs. Trim the *Props bags toward composition (children slots) and remove the legacy `body` prop. Document the one supported way to make a card actionable.
- [MEDIUM] **The contrast/validation gate that the system showcases is too shallow to cover the combinations users actually see**
  - evidence: screenshots/02-contrast-overview.png footer 'Pairs audited: 7'; the DS ships status tones across Button (tone error/warning/success/info), Card statusMessage, HelperText states, Badge, Toast — far more than 7 fg/bg pairs, and the known HCW warning 1.19 Fail (variables.css HCW block) ships unfixed.
  - so what: Auditing 7 pairs creates false confidence: the self-flagging Contrast story is genuinely excellent but only spot-checks a handful, so real low-contrast states (e.g. warning tone surfaces, disabled tokens like primary-disabled #0000ff50) reach users unverified. For an org positioning as a design-systems authority, a 7-pair gate undersells and under-protects.
  - fix: Expand the contrast gate to enumerate every semantic fg/bg token pair per theme (drive it from variables.css programmatically), fail CI on any AA miss, and fix the shipped HCW warning pair before re-promoting the high-contrast themes.
- [LOW] **Loading and disabled states are visually communicated but not consistently narrated, and `loading` silently suppresses interaction without telling the user why**
  - evidence: Button.tsx:202 sets aria-busy on loading and :247 disables the control, but renders no text/spinner change or sr announcement of 'loading'; Card.tsx:393-400 loading skeleton is good (role=status + aria-label t('card.loading')). The two disagree on whether loading is announced.
  - so what: A user who clicks Submit and the Button enters loading sees the same label, gets no 'submitting…' feedback, and the control just stops responding — sighted users may double-click, AT users get only aria-busy with no human-readable status. Inconsistent loading treatment across primitives means inconsistent perceived responsiveness.
  - fix: Standardize a loading affordance: swap/append a spinner + visually-hidden 'Loading' (localized) on Button to match Card's pattern, and document loading/disabled as a first-class state in the field/control specs so all interactive primitives behave the same.

**Top priorities:**
  1. Fix the FormField/CheckboxField broken aria wiring (cloneElement the id + aria-describedby + aria-invalid onto the control) and add a rendered-DOM test so the documented contract can't silently lie — this is the single highest-impact usability+a11y defect because it affects every form built on the molecule.
  1. Converge the 4+ input families onto one canonical field architecture with a single `error: string` contract, then refactor ContactForm onto it as proof-of-use; quarantine/deprecate the shadcn components/ui and legacy Inputs/* duplicates to remove source-of-truth ambiguity.
  1. Close the interaction-affordance traps: make disabled Button-as-link non-navigable and out of tab order (Button.tsx:226-240), and unify status semantics so errors are role=alert/assertive and dismiss copy is localized (AlertBanner.tsx:38-64).

## a11y-expert  —  62/100
**Verdict:** A genuinely sophisticated a11y foundation (computed-contrast story, forced-colors per component, roving-tabindex Tabs, live-region Toast) undercut by shipped WCAG failures the system's own tooling already detects, and structural bugs in the highest-traffic primitives (Card link-mode, Button disabled-link, Modal focus trap).

**Bottom line:** As an accessibility expert I would NOT certify this system at the 'design-systems authority' bar today, despite real craft. The foundations are above-average (computed-contrast story, forced-colors tokens, roving-tabindex Tabs, live-region Toast, global focus-visible, skip-link/landmark), but a design system is judged by its primitives under keyboard and screen reader, and three of those primitives have concrete, sanctioned-by-tests defects (Card link nesting, Button disabled-link navigates, Modal trap is inert-only with a hollow test) plus a shipped WCAG contrast failure the team's own tooling flags. The gap is closeable and mostly localized: fix the HCW warning token, gate it, harden Card/Button/Modal, and broaden the 7-pair contrast gate to rendered components. Lighthouse 100 here is a floor, not a verdict.

**Strengths:**
- Toast is a textbook accessible live region: persistent mount, message swapped in/out, tone-correct assertive vs polite, icon marked aria-hidden, plus aria-atomic _(nextjs-app/shared/components/Toast/Toast.tsx:42-87 (role/aria-live derived from tone; comment explains why it does NOT use AnimatePresence mount/unmount))_
- Global :focus-visible exists app-wide (focus is NOT opt-in), and skip-link + a real <main id="main-content"> landmark are wired _(app/globals.css:89 ':focus-visible{outline:2px solid var(--color-primary)}'; NextLayout.tsx:33-35 <SkipLink href="#main-content"/> + <main id="main-content">)_
- forced-colors / Windows High Contrast handled at the token layer with system color keywords (CanvasText, Highlight, LinkText), not just left to UA defaults _(variables.css:595-618 forced-colors block maps semantic tokens to CanvasText/Canvas/Highlight; utilities.css:54-58 swaps focus outline to 'Highlight' under forced-colors)_
- Tabs implements correct APG roving tabindex (active=0, others=-1) with Arrow/Home/End and Enter/Space, and ships a getTabPanelProps helper so consumers wire aria-controls/labelledby correctly _(Tabs.tsx:159-171 (tabIndex={isActive?0:-1}, aria-selected, aria-controls) and :204-212 getTabPanelProps)_

**Findings:**
- [HIGH] **HCW high-contrast theme ships a 1.19:1 WCAG FAIL for warning text — and the design system's OWN contrast tool already flags it**
  - evidence: variables.css:534 '--color-warning:#000' + :536 '--color-warning-text:#041b23' (near-black on black); screenshot 02 'High contrast (white)' row 'Warning text on warning' = 1.19 / Fail; 'Pairs audited: 7'
  - so what: A high-contrast theme exists specifically for low-vision users, yet its warning state is invisible to the exact audience it serves — the most damaging possible failure mode for an a11y feature. Shipping a defect your own foundations story renders in red is an authority-credibility problem, not just a bug.
  - fix: Set '--color-warning-text' (and '--color-warning-contrast') to a value with >=4.5:1 against the HCW warning background. Since HCW collapses warning to #000, render warning surfaces as a bordered/iconed chip on Canvas with CanvasText, or set warning-bg to #fff and keep text #000. Then add the failing pair to the CLI contrast gate so it cannot regress.
- [HIGH] **Card link-mode nests interactive elements (Tabs buttons, action Buttons, role=tabpanel, role=alert) inside an anchor**
  - evidence: Card.tsx:402-411 innerContent includes tabSection + footerActions + statusMessageBlock; :433-442 wraps that exact innerContent in <Link href={link}> (an <a>); used in pSEO pages (PseoLeafPage/PillarPage/IndexPage)
  - so what: Interactive controls inside an <a> is invalid HTML and a real AT failure: nested focusable tab stops with conflicting roles, button activations that also trigger navigation, and screen readers announcing a link that contains a tablist/alert. Keyboard users cannot reliably operate actions inside a link card. This is the system's most-composed surface, so the blast radius is large.
  - fix: Make link-mode and interactive content mutually exclusive: when 'link' is set, refuse (dev-warn) to render actions/tabs/interactive children, OR switch to the 'card with a stretched-link title' pattern (a single <a> over the heading using ::after to expand the hit area) so the card stays a <div> and inner controls remain real siblings. Add a dev-mode invariant in Card.tsx that throws/warns on link + (actions||tabs).
- [HIGH] **Button 'disabled' link still navigates and stays in the tab order**
  - evidence: Button.tsx:226-240 link branch sets aria-disabled={disabled||loading} but keeps href live, adds no onClick/onKeyDown suppression and no tabIndex={-1}; sanctioned by tests Button.test.tsx:150 and Button.a11y.test.tsx:274
  - so what: aria-disabled communicates 'disabled' to AT but the control still activates on click/Enter and remains focusable — a keyboard/AT user is told it's off yet it fires. WCAG 4.1.2 name/role/value mismatch and a functional trap for the exact users relying on the ARIA. The team's own Button docs tout disabled-vs-aria-disabled handling, so this contradicts the published contract.
  - fix: In the disabled link branch, drop href (render as <span role="link" aria-disabled="true"> or <a> without href), set tabIndex={-1}, and add onClick={e=>e.preventDefault()} + onKeyDown guard for Enter. Update the two tests to assert the link does NOT navigate and is not tabbable when disabled.
- [MEDIUM] **Modal focus trap depends entirely on inert with no Tab/Shift+Tab wrap, and the 'Tab cycles' test never asserts wrapping**
  - evidence: Modal.tsx:105-142 sets inert on #main-content and focuses first focusable; there is no keydown handler for Tab to wrap last->first/first->last; Modal.a11y.test.tsx:270-301 tabs ONCE then only asserts buttons toBeInTheDocument()
  - so what: In the production app inert resolves (NextLayout provides #main-content), so it mostly works there — but the modal portals to document.body and any consumer outside NextLayout (Storybook, the legacy Vite app, embedded usage) hits the fragile fallback chain (#__next is Pages-Router-only; last resort document.body.firstElementChild). With no Tab-wrap fallback, focus can escape entirely, and the test that claims to cover this proves nothing. For a dialog primitive in a design-systems authority, the trap must not be environment-dependent.
  - fix: Add an explicit focus-wrap: on Tab at the last focusable move to first, on Shift+Tab at the first move to last (compute focusables on each keydown). Keep inert as defense-in-depth. Broaden the focusable selector (add details, audio[controls], video[controls], [contenteditable]) and exclude [aria-hidden]/disabled. Rewrite the test to assert document.activeElement actually returns to the first element after tabbing past the last.
- [MEDIUM] **Inputs overloads the 'required' indicator to signal 'has error' and routes the error message into the label's tooltip**
  - evidence: Inputs.tsx:165-167 <Label required={hasError} tooltipText={errorMessage} ...> — required is driven by hasError, and the error text is passed as a label tooltip
  - so what: Two distinct a11y problems: (1) a field in an error state is visually/semantically marked 'required', conflating two orthogonal states and misinforming AT users about what's wrong; (2) the error is delivered via a label tooltip, which is not reliably exposed to screen readers or operable by keyboard. The component DOES correctly wire aria-describedby to HelperText (which has role=alert at HelperText.tsx:64), so the tooltip is redundant noise that can mislead. WCAG 3.3.1/3.3.3.
  - fix: Decouple the states: pass the component's real 'required' prop to Label (don't reuse hasError), and remove tooltipText for errors — rely on the already-correct aria-describedby + HelperText role=alert path. Reserve the asterisk strictly for genuinely required fields.
- [MEDIUM] **HCB high-contrast overrides for badge/toast are mis-nested inside the .themeHCW selector, so they never apply in HCB**
  - evidence: variables.css:499-518 — '.themeHCB .badge' and '.themeHCB .toast { forced-color-adjust:none }' sit inside the '.themeHCW, .themeHCW :root {' block opened at :499
  - so what: In the High-Contrast-Black theme, the intended forced-color-adjust:none opt-out for badges/toasts is dead (a .themeHCW .themeHCB descendant combinator that almost never matches). HCB users may get forced-colors flattening on components the team explicitly tried to protect, and the copy-paste nesting signals the high-contrast themes aren't being visually regression-tested per theme.
  - fix: Move the two '.themeHCB ...' rules out to top level (or into a real '.themeHCB { ... }' block). Add a Storybook visual story per high-contrast theme for Badge and Toast so theme-scoped CSS regressions surface.
- [MEDIUM] **Automated contrast gate covers only 7 token pairs against ~150 components, leaving most real foreground/background combinations unverified**
  - evidence: screenshot 02 'Pairs audited: 7'; Contrast story enumerates a fixed semantic-pair list, not component-rendered combinations; EVIDENCE.md notes Lighthouse a11y 100 but automation catches ~30-40%
  - so what: The 7-pair gate is what let the HCW 1.19 failure ship 'green' at the component level — body/title/link/error/warning on canvas is a tiny slice. Pairs like muted-on-light-bg, disabled-placeholder, badge tones, status messages, onDark/onBrand button surfaces, and the dark-theme blue body copy (--main-body-copy-color:#6fa8ff) are unaudited. For a system positioning on rigor, 7 pairs is a contrast smoke test, not a gate.
  - fix: Expand the contrast catalog to every semantic fg/bg pairing per theme (including disabled, placeholder, badge/status tones, and the three Button surfaces), fail CI on any <4.5:1 (or <3:1 for >=24px/bold and UI components per 1.4.11), and assert the count climbs from 7. Pair it with axe color-contrast run against rendered stories, not just token math.
- [LOW] **HelperText status icons (incl. error) are not hidden from assistive tech, unlike the consistent aria-hidden pattern used in Button/Toast**
  - evidence: HelperText.tsx:57-67 wraps stateIcons in <span className={styles.icon}> with no aria-hidden, and the Phosphor icons (e.g. WarningCircle at :40) carry no aria-hidden; contrast with Toast.tsx:9-12 and :81 which set aria-hidden on every icon
  - so what: Decorative status icons can be announced as graphics or add stray output to the role=alert error region, so a screen-reader user may hear the warning glyph described before/around the actual message. Minor, but it's an inconsistency in an otherwise disciplined icon-accessibility convention and sits inside a live region where extra noise is most disruptive.
  - fix: Add aria-hidden="true" to the icon wrapper span in HelperText.tsx (and/or pass it to the Phosphor icon), matching the Toast/Button decorative-icon convention.

**Top priorities:**
  1. Fix the shipped WCAG contrast failure the system already detects: raise HCW warning-text contrast to >=4.5:1 (variables.css:534-536) AND add that pair to the contrast gate so it can't regress — an invisible warning in the low-vision theme is the worst failure an a11y feature can have.
  1. Eliminate interactive-in-anchor and disabled-but-active patterns in the two most-used primitives: make Card link-mode exclude actions/tabs or switch to a stretched-link (Card.tsx:433-442), and make Button's disabled link actually inert (drop href, tabIndex=-1, preventDefault at Button.tsx:226-240).
  1. Make the Modal focus trap environment-independent: add explicit Tab/Shift+Tab wrap as a fallback to inert (Modal.tsx:105-142) and replace the hollow 'Tab cycles' test with one that asserts focus actually wraps (Modal.a11y.test.tsx:270-301).

## senior-developer  —  68/100
**Verdict:** Engineering fundamentals are strong (excellent orthogonal API conventions, real a11y test depth on flagship components, correct RSC discipline on most leaf primitives), but the foundation is undermined by a navigable "disabled" link in the flagship Button, stated rules that the lint/type pipeline does not enforce (293 inline styles, 225 hex, noUncheckedIndexedAccess off against 110 dynamic class lookups), and an unresolved two-component-system fork.

**Bottom line:** Would-I-ship: yes with required changes, not as-is. This is clearly above a generic-startup bar - the variant x tone x size x surface API, the server-first leaf primitives, the deep a11y tests on flagships, and the token/contract automation are senior-grade and genuinely impressive. But against the design-systems-authority bar the team claims, three things block a clean sign-off: a flagship Button whose 'disabled' link still navigates, a rulebook the pipeline does not enforce (so the rules are aspirational), and an unfinished shadcn fork that destroys single-source-of-truth. Fix the disabled-link and turn the prose rules into lint/type gates and this jumps from a solid 68 to elite. The bones are excellent; the enforcement and the last 10% of rigor are what's missing.

**Strengths:**
- variant x tone x size x surface is a genuinely excellent, orthogonal API - the real deal, not table stakes _(Button.tsx:8-25,92 - ButtonVariant (weight) and ButtonTone (semantic color) are decoupled; ButtonAsButton/ButtonAsLink (Button.tsx:60-77) give compile-safe polymorphism via `href?: never` mutual exclusion; tone->icon auto-mapping (TONE_TO_STATUS :20-25, :165-175).)_
- RSC discipline on leaf primitives is actually correct - the '87% use-client' framing overstates it _(Title.tsx, Text.tsx, Badge.tsx, Link.tsx, Grid.tsx, FlexBox.tsx, Icon.tsx ship WITHOUT 'use client' and use only forwardRef (Text.tsx:36, Title.tsx:46, Icon.tsx:1). The most-used atoms are server-renderable; only Button/IconButton are mis-marked.)_
- A11y test depth on flagship components is substantive, not box-ticking _(Dedicated .a11y.test.tsx tier: Modal.a11y 433 lines, Button.a11y 392, Switch.a11y 334, Checkbox.a11y 293; 7/8 invoke real axe(). Button.test.tsx:197-267 covers icon-only-name warnings, aria-busy on/off, tooltip->aria-label fallback, target=_blank->rel.)_
- Token tooling and contract automation are well beyond typical - real DS infrastructure _(package.json:86-152: check:contract-drift, sync:contract-props, validate:components, build:tokens (DTCG export + Tailwind sync + relationship graph), check:bundle-budgets, agent:eval. Contrast story self-flags WCAG failures (EVIDENCE.md sec 8).)_

**Findings:**
- [HIGH] **Flagship Button renders a navigable 'disabled' link: aria-disabled set but href kept and no pointer-events/click suppression**
  - evidence: Button.tsx:226-240 - disabled link path sets aria-disabled (:234) but keeps `href={href}` (:232) and adds no onClick/keydown guard. Button.module.css:125-142 gives the [aria-disabled="true"] anchor only `cursor:not-allowed` + opacity 0.5; `pointer-events:none` exists ONLY for .loading (:148), not disabled. So a disabled link still navigates by mouse and keyboard.
  - so what: For a DS that ships a whole accessibility doc section on 'disabled vs aria-disabled' (EVIDENCE.md sec 8), the canonical primitive contradicts its own guidance: SR users hear 'dimmed/unavailable' yet activation works. This is the highest-blast-radius correctness bug because Button is the most-imported interactive component.
  - fix: In the disabled-link branch either drop href entirely (render a <span role="link" aria-disabled>) or add `onClick={e=>{if(disabled||loading)e.preventDefault()}}` plus `tabIndex={-1}` and `pointer-events:none` on the [aria-disabled] anchor. Add a Button.a11y test asserting a disabled link does not fire navigation.
- [HIGH] **Stated rules are unenforced: 293 inline styles + 225 hex pass lint clean (only 1 of 294 inline styles is the allowed backgroundImage)**
  - evidence: CLAUDE.md bans inline styles 'except dynamic backgroundImage' and hardcoded colors. Verified: 294 `style={{}}` in non-test components, of which exactly 1 references backgroundImage and 0 set a CSS-variable passthrough; 225 hex literals in *.module.css. eslint.config.mjs:68-80 has no forbid-dom-props, no no-restricted-syntax for style, no color rule - the rules live only in prose. Hotspots: pages/Work/*Page.tsx (HelsinkiDesignSystemPage 12, SapBuildAppsPage 10) plus Card's bodyStyle/headStyle passthrough (Card.tsx:123-125,250,367).
  - so what: A DS that publishes 'CSS Modules only / tokens only' as law but ships ~293 violations with a green pipeline has decoration, not guardrails. Contributors and the LLM agents this repo explicitly targets will pattern-match the violations as sanctioned.
  - fix: Add eslint no-restricted-syntax flagging JSXAttribute[name.name='style'] with an allowlist-comment escape, and wire stylelint-declaration-strict-value (already a devDependency, package.json:319) for color/length against a ratcheting baseline rather than demanding zero overnight.
- [HIGH] **Two component systems shipped side by side: 13 tracked shadcn primitives (rsc:false, lucide) vs the bespoke ~150 (CSS Modules, Phosphor)**
  - evidence: components/ui/ has 13 files (accordion,button,dialog,dropdown-menu,select,tabs,tooltip,...) governed by root components.json ('style':'new-york','rsc':false,'iconLibrary':'lucide'). The bespoke system uses Phosphor (Icon.tsx:2) and its own Button/Tabs/Tooltip/Modal. Both git-tracked; Storybook carries ShadcnMigration + MigrationDecisionBoard stories (EVIDENCE.md sec 6).
  - so what: Source-of-truth ambiguity is the cardinal sin for a design system. Two Buttons, two icon libs, two RSC stances mean consumers cannot know which to import, bundles risk carrying both Phosphor and lucide, and the 'authority' positioning is contradicted by an unfinished migration sitting in main.
  - fix: Pick the single source of truth and record it in a dated ADR: either complete the shadcn migration or remove components/ui/ + components.json from the tracked tree until it is funded. Do not leave a half-fork on the default branch.
- [MEDIUM] **Button and IconButton carry 'use client' with zero client APIs, dragging Icon + the Phosphor registry into the client bundle**
  - evidence: Button.tsx:1 and IconButton.tsx:1 are 'use client', but grep for useState/useEffect/useRef/useContext/useReducer/window/document returns NONE in both. The only effect is a process.env.NODE_ENV dev warn (Button.tsx:123-139) which runs fine in an RSC. Button imports Icon (Button.tsx:6); Icon is itself server-capable (Icon.tsx:1 has no directive), so Button's directive is what forces Icon + resolvePhosphorIcon client-side.
  - so what: The two most-imported interactive primitives needlessly grow every consumer's client bundle and erase the RSC win correctly earned on Title/Text/Badge. It also teaches the wrong default ('interactive == use client'), which is false for a stateless forwardRef button.
  - fix: Remove 'use client' from Button.tsx and IconButton.tsx (onClick handlers come from already-client consumers), verify Storybook/tests pass, then audit the remaining 123 directives for the same stateless pattern.
- [MEDIUM] **noUncheckedIndexedAccess is off while 110 dynamic styles[key] lookups depend on it - missing/typo'd CSS classes fail silently**
  - evidence: tsconfig.json:11 strict:true but no noUncheckedIndexedAccess. Next's ambient type is `{ readonly [key:string]: string }` (node_modules/next/types/global.d.ts), so styles[variant] types as string (never undefined) even for an unauthored key. 110 styles[...] lookups in non-test components, e.g. Button.tsx:188-191 styles[variant]/styles[tone]/styles[size]/styles[surface]. A new tone value or a typo joins `undefined` into className with no compile error.
  - so what: For a token-driven DS, a silently-dropped variant class is exactly the bug that ships a visually-broken component past a green typecheck. The flag converts a whole category of 'the class just didn't apply' runtime mysteries into compile errors.
  - fix: Enable noUncheckedIndexedAccess and use `styles[variant] ?? ""` (or generate per-module .d.ts with literal keys - only Layout.module.css.d.ts exists today). Also bump target from ES2017 (tsconfig.json:3) to ES2022 for React 19 / Node >=22.12 (package.json:6).
- [MEDIUM] **Card is a ~40-prop god component that nests interactive Buttons/Tabs inside an anchor in link mode (invalid HTML + a11y)**
  - evidence: Card.tsx:28-130 declares ~40 props (title/subtitle/description/body-legacy/tabs/badge/actions/statusMessage/cover/loading/link/onClick/bodyStyle/headStyle). Link mode (Card.tsx:433-442) wraps the full innerContent in <Link>; innerContent (:402-411) includes footerActions=Buttons (:202-216) and tabSection=Tabs (:191-200) -> <a> containing <button>/tablist. Also bordered defaults true (:147) while variant defaults 'outlined' (:148) - two overlapping border switches.
  - so what: Interactive descendants of an anchor are invalid HTML and produce nested-focusable, unpredictable activation for keyboard/SR users. The 40-prop surface also makes Card impossible to type-narrow safely (link + actions are nonsensical together but compile fine) - the opposite of the discriminated-union rigor Button shows.
  - fix: Split into composable subcomponents (Card.Header/Body/Actions/Media) or, minimally, make `link` a discriminated union forbidding actions/tabs/onClick at the type level (mirror ButtonAsLink's href?:never). Drop legacy `body` and collapse bordered+variant into one.
- [MEDIUM] **Test coverage is bimodal: deep on flagships, tautological smoke on form controls - only 19 of ~150 dirs run axe**
  - evidence: RadioGroup.test.tsx is 10 lines whose sole assertion is `expect(document.body).toBeTruthy()` - a tautology that cannot fail and tests nothing about roving tabindex, aria-checked, or fieldset/legend. Same shape for Radio, Progress, Divider, TextArea, Spinner. Verified: 24 test files invoke axe() across only 19 unique component dirs of ~150; 148 .test.tsx exist but the long tail is smoke-only.
  - so what: A form control is precisely where keyboard/ARIA bugs hide, and a tautological test is worse than none: it shows green in coverage and lulls reviewers. For a DS sold on accessibility, axe on ~13% of components is a credibility gap against the elite bar.
  - fix: Define a minimum bar for interactive components (axe + keyboard interaction + ARIA-state assertions) and gate the WIP->beta promotion on it (the lifecycle in AGENTS.md:79-85 already exists - wire the gate). Replace toBeTruthy() smoke tests on form controls first.
- [LOW] **react-icons floats at ^5.6.0 against a documented hard-pin to 5.5.0, with react-icons/si on 12 work pages**
  - evidence: package.json:217 '"react-icons": "^5.6.0"'; installed resolves to 5.6.0. CLAUDE.md gotcha: 'pinned at 5.5.0 (5.6 drops Adobe SI icons on work pages)'. react-icons/si is imported on 12 files under pages/Work/* (VertaaUX, NewThingsCo, Rhythmguard, Tulli, LlmComponentSchema, ...). The ^ also permits any future 5.x.
  - so what: Either the gotcha is stale (the doc lies to every contributor) or it is real (the floating range can break Simple-Icons rendering on portfolio pages at the next lockfile refresh). Both are unacceptable for a team whose product is the correctness of its own component metadata.
  - fix: Resolve the contradiction: if 5.6 is safe, delete the gotcha and pin exact 5.6.0; if not, pin exact 5.5.0. Drop the ^ either way so the constraint matches the stated policy, and add a smoke test importing one react-icons/si glyph.

**Top priorities:**
  1. Fix the navigable disabled-link in Button (Button.tsx:226-240 + Button.module.css:125-142): strip href or preventDefault+tabIndex=-1+pointer-events:none, and add an a11y test - the flagship primitive must obey its own disabled-vs-aria-disabled doctrine.
  1. Make the stated rules executable: wire eslint no-restricted-syntax for inline style and stylelint-declaration-strict-value for color/length on a ratcheting baseline, and enable noUncheckedIndexedAccess - close the gap between prose law and a green pipeline (293 inline styles, 225 hex, 110 unchecked lookups).
  1. Resolve the two-system fork: commit to bespoke-vs-shadcn in a dated ADR and either finish or remove components/ui/+components.json from main - a design-systems authority cannot ship two Buttons and two icon libraries on its default branch.

## dev-architect-cto  —  62/100
**Verdict:** Architecturally ambitious and unusually well-governed on paper, but it is a single-author artifact whose entire quality apparatus runs nowhere but one person's machine, with two unreconciled component systems and large typecheck/lint blind spots over its most critical code.

**Bottom line:** As a CTO deciding whether to scale this to a team and ship it to clients: not yet, but it is close, and the gap is enforcement and consolidation rather than capability. The architecture, token pipeline, and governance design are genuinely top-decile for a design system, but they currently exist as a brilliant solo artifact: one author wrote 88% of commits, the CI that would enforce 24 gates does not run, the most critical code (scripts, tests, stories) escapes both typecheck and lint, and two unreconciled component systems plus three token namespaces require tacit knowledge only the author holds. Fix the bus-factor (real gate off the author's laptop + a second maintainer), pick one component system, and unify tokens, and this becomes a defensible design-systems-authority asset. Ship the governance to the runner, not just the laptop.

**Strengths:**
- 24-gate PR pipeline plus a self-healing generated-drift gate is genuinely senior governance design _(.github/workflows/pr-validation.yml:12-39 (build:tokens → contract-drift → check:generated → contrast → typecheck → lint → tests → build); scripts/design-system/check-generated.mjs regenerates tokens and `git diff HEAD` fails on drift)_
- Token source-of-truth is single-direction (CSS → DTCG → Tailwind), not hand-maintained in parallel _(scripts/design-system/export-dtcg-from-css.mjs reads the catalog derived from variables.css; build:tokens chains extract→export-dtcg→build→sync-tailwind→relationship-graph (package.json:94))_
- Dependency-supply-chain hygiene is deliberate: pinned Node engine, broad security overrides, audit tooling _(package.json:5 engines node>=22.12.0; overrides for undici/path-to-regexp/dompurify/serialize-javascript/fast-xml-parser (package.json:234-263); scripts security:audit:triage, deps:report, dependency-risk-report.mjs)_
- DS introspection/agent layer (manifest, zod catalog, relationship graph) is a real futureproofing asset for tooling and onboarding _(nextjs-app/shared/foundations/dist/{agent-manifest.json (1.2MB),component-catalog.zod.ts,relationship-graph.json,tokens-manifest.json}; build:agent-blocks, build:zod-catalog (package.json:95,124))_

**Findings:**
- [BLOCKER] **Two component systems are both load-bearing in production, and the shadcn copy is permanently forked (defeats the point of shadcn)**
  - evidence: nextjs-app/shared/components/interactive/index.ts:1-52 re-exports Dialog/Accordion/Tabs/DropdownMenu from `@/components/ui/*` (shadcn, Tailwind+CVA) but Tooltip from `@dt/Tooltip` (CSS Modules) — one barrel, two styling architectures. components/ui/button.tsx has bespoke `// Studio variants` (primary/inverse/minimal, lines ~30-33) grafted into the cva, so it can never be re-pulled from the shadcn registry. Bespoke @dt/Tabs (platinum uplift) AND shadcn Tabs both exist.
  - so what: Through a CTO lens this is the #1 scalability blocker: there is no actual migration strategy, just two parallel inventories that drift independently. A new engineer cannot answer 'which Button do I use?' Every interactive primitive must be maintained, themed, a11y-tested, and Figma-synced twice. A 'design-systems authority' shipping two un-unified systems undercuts the core claim. The MigrationDecisionBoard stories document the indecision rather than resolving it.
  - fix: Pick one runtime for interactive primitives and commit. Either (a) absorb the 3-4 shadcn primitives into the bespoke CSS-Module DS and delete components/ui + components.json, or (b) make shadcn+Tailwind the explicit substrate for behavior-heavy primitives and stop hand-rolling Dialog/Tabs/Tooltip in CSS Modules. Write the decision as an ADR with a dated end-state and a deletion deadline; un-fork or rename the shadcn button so 'Studio variants' aren't masquerading as vendored code.
- [BLOCKER] **The entire quality apparatus is enforcement theater: 88% single-author, CI does not gate, no pre-commit hook**
  - evidence: git log: 1001/1140 commits (88%) by one human, remaining 139 are dependabot (`git shortlog -sne`). User's standing context: GitHub Actions CI is quota-dead across all repos, PR checks always fail/hang. .husky/ contains only pre-push (no pre-commit; scripts/precommit-guard.sh exists but is unwired). pre-push only runs validate:components+typecheck+lint+lint:css, and only when paths match nextjs-app/shared|scripts/design-system.
  - so what: This is the defining bus-factor finding. The impressive 24-step pr-validation.yml never executes as a merge gate, so all 24 gates collapse onto one person remembering to run them locally. Contract-drift, contrast, token-generation, bundle-budgets, agent-eval — none of it is mechanically enforced. To onboard a team or hand a client a maintainable system, the gate must live off the author's laptop. Today the system is brilliant precisely because one person holds all the tacit rules in their head.
  - fix: Restore real merge enforcement: move the gate to a runner that isn't quota-blocked (self-hosted runner, or a paid CI minute budget, or a mandatory local `npm run parity:verify` enforced by a wired pre-commit/pre-push that runs the FULL gate, not a 4-command subset). Wire the existing precommit-guard.sh. Add a second reviewer/committer to break the 88% concentration. Document the gate as a CONTRIBUTING runbook so it survives the author.
- [HIGH] **tsconfig and eslint both blind-spot the most critical code: 399 test/story files skip typecheck; the entire scripts/ tree is neither typechecked nor linted**
  - evidence: tsconfig.json:65-68 excludes **/*.test.ts(x) and **/*.stories.ts(x); :75 excludes scripts/design-system/**/*.ts. Counts: 215 test + 177 story files escape `tsc --noEmit`; 7 design-system scripts excluded. eslint.config.mjs:34 ignores `scripts/` entirely, and the config sets @typescript-eslint/parser with NO parserOptions.project, so type-aware lint rules never run anywhere.
  - so what: The token build, contract-drift checker, agent-manifest generator, release-gate — the load-bearing automation the whole DS trusts — runs with the least static safety in the repo. A type error in a script silently ships; a type error in a story or test only surfaces if that test happens to execute. For a system whose value proposition is correctness and governance, the governance code itself is the least guarded.
  - fix: Add a tsconfig.scripts.json (or include scripts/design-system in a project reference) and run it in typecheck. Add a tsconfig.test.json covering *.test/*.stories and add `tsc -p tsconfig.test.json --noEmit` to the gate. Stop ignoring scripts/ in eslint (or lint it with a node-tuned config). Optionally enable type-aware linting via parserOptions.project for src.
- [HIGH] **Three overlapping color-token namespaces with a name collision and a hardcoded fallback that can silently drift**
  - evidence: variables.css defines `--color-primary:#041b23`. app/tailwind.css:197 redefines `--primary: var(--color-primary, #041B23)` (hardcoded fallback duplicates the value), then @theme inline (app/tailwind.css ~line 40) defines `--color-primary: var(--primary)` — same custom-property name as variables.css, different chain. shadcn vocabulary (--card,--popover,--sidebar-*,--chart-1..5,--ring,--input,--accent,--destructive) exists ONLY in app/tailwind.css and is disjoint from variables.css.
  - so what: `--color-primary` resolved at a node depends on cascade origin, which is a comprehension landmine for anyone but the author (bus-factor). The `#041B23` literal at tailwind.css:197 is a second source of truth for the brand color: change variables.css and this fallback silently goes stale. Two disjoint token vocabularies must be kept in sync by hand, which is exactly the drift the DTCG pipeline was built to prevent — except the shadcn half isn't in the pipeline.
  - fix: Collapse to one namespace: either feed the shadcn raw tokens (--primary,--card,--popover,...) from the same generated DTCG output (extend build:tokens/sync-tailwind-theme.mjs to emit them) so there is one source, or drop the @theme `--color-*` re-alias and bind Tailwind directly to the variables.css tokens. Remove the hardcoded #041B23 fallback. This is the concrete unblocker for resolving the two-systems problem above.
- [HIGH] **react-icons floating range contradicts the documented pin, leaving a future-install footgun across 16 SI-dependent pages**
  - evidence: package.json:217 `react-icons: ^5.6.0` vs CLAUDE.md gotcha 'pinned at 5.5.0; 5.6 drops Adobe SI icons on work pages'. VERIFIED: installed 5.6.0 still resolves react-icons/si and all 10 SI icons actually used (SiFigma,SiReact,SiTypescript,SiStorybook,SiTailwindcss,SiSketch,SiSlack,SiNpm,SiNodedotjs,SiGithubactions) resolve fine. 16 files import react-icons/si.
  - so what: Not a live breakage today (the doc's specific claim is now stale/inaccurate at 5.6.0), but the `^` means the next clean `npm install` can float to 5.7+ which could remove icons, taking out 16 work/case-study pages — the team's own portfolio. More corrosively for bus-factor: the governing doc actively lies about the installed state, so the next engineer trusts CLAUDE.md and is wrong. A 4th icon library (simple-icons ^16.23.0 alongside @phosphor-icons/react, lucide-react, @storybook/icons) compounds the surface.
  - fix: Decide and reconcile: if 5.6 is safe (it is, today), update the CLAUDE.md gotcha to match reality and pin exactly (`5.6.0`, no caret) so installs are reproducible. Add a tiny CI/local guard that imports the SI icons used on Work pages and fails if any are missing (the breakage you actually fear). Rationalize the four icon libraries to a documented set.
- [HIGH] **Disabled Button-as-link still navigates and stays keyboard-activable — a correctness/a11y bug in the single most-used primitive**
  - evidence: nextjs-app/shared/components/Button/Button.tsx:226-240: the link branch sets only `aria-disabled={disabled||loading}` but keeps `href`, adds no onClick suppression, no tabIndex=-1, no disabled-styling class wiring for pointer-events. The button branch (line 247) correctly uses native `disabled`. The component's own docs teach disabled-vs-aria-disabled (evidence pack screenshot 04).
  - so what: Through the robustness lens, a bug in Button is a bug everywhere — it is the most-imported component. A 'disabled' CTA link still navigates on click and via Enter, which is both a functional defect and a WCAG issue, and it ships in a DS that explicitly documents the correct pattern. That gap between documented intent and shipped behavior is exactly what erodes trust when a client adopts the system.
  - fix: In the link branch, when disabled||loading: drop href (render without it or as a span/role), set tabIndex={-1}, add aria-disabled, attach an onClick that preventDefault+stopPropagation, and apply a class that sets pointer-events:none. Add a test asserting a disabled link does not fire navigation/onClick and is not in the tab order.
- [MEDIUM] **target ES2017 and noUncheckedIndexedAccess OFF despite 25 dynamic styles[variant] lookups — old posture for a React19/Next16 stack**
  - evidence: tsconfig.json:2 `target:ES2017`; no `noUncheckedIndexedAccess` (grep count 0). 25 occurrences of `styles[<dynamic>]` in nextjs-app/shared/components (e.g. Button.tsx:188-191 styles[variant]/styles[tone]/styles[size]/styles[surface]).
  - so what: ES2017 forces downleveling of async/await and spread that a Node>=22 / R19 / Next16 audience runs natively, inflating bundle and masking the real platform baseline. noUncheckedIndexedAccess OFF means every `styles[variant]` is typed as always-present string; a typo or a tone with no CSS class yields `undefined` joined silently into className with no compile error — a whole class of theming bugs the type system could catch for free given how pervasive the pattern is.
  - fix: Bump target to ES2022 (matches engines node>=22.12). Turn on noUncheckedIndexedAccess and fix the resulting ~25 lookups with a typed helper (e.g. cx(styles, key)) — this hardens the core class-composition pattern across the whole DS.
- [MEDIUM] **Hybrid Next16/Vite-legacy plus git-tracked debug/backup clutter inflate the maintenance and onboarding surface**
  - evidence: git ls-files confirms tracked root clutter: CLAUDE.md.backup, debug-env.js, check_missing_tests.sh, PHASE_2_BREAKING_CHANGES.md, PHASE_2_IMPLEMENTATION_SUMMARY.md, TIER_1_*_ANALYSIS.md, TIER_1_CSS_CHANGES.md, claude-desktop-blank-screen-bug-report.md, test-results-summary.md, generate-agents.md, generate-claude.md (~20 stray root docs/scripts). tsconfig excludes src + api-legacy-vercel-functions + shared_backup_20251123 (legacy Vite app + legacy serverless still in tree).
  - so what: Bus-factor and onboarding: a new contributor cannot distinguish source-of-truth from scratch files. CLAUDE.md.backup next to CLAUDE.md is an ambiguity trap; debug-env.js shipping in git is a (small) footgun. The legacy Vite/serverless surface is dead weight that still has to be reasoned around (excluded from typecheck, so it can rot invisibly). None of this is fatal, but it is the kind of entropy that compounds and signals solo-artifact rather than team-ready.
  - fix: Delete or move the stray root files to a gitignored /private or /docs/archive; add them to .gitignore where they are scratch. Decide the legacy Vite app's fate (archive to a tag and remove from the working tree, or document it as frozen). Add a lightweight 'repo hygiene' check (no *.backup, no debug-*.js tracked) to the gate.

**Top priorities:**
  1. Move quality enforcement off the single author's machine: restore a real merge gate (non-quota CI runner or a wired pre-commit/pre-push running the FULL parity:verify), wire the dormant precommit-guard, and add a second committer — today 88% single-author + dead CI means the 24-step pipeline gates nothing.
  1. Resolve the two-component-systems problem with a written ADR and a deletion deadline: unify interactive primitives onto one runtime, un-fork the shadcn button, and collapse the three color-token namespaces (variables.css / app/tailwind.css --primary / @theme --color-*) into the single DTCG-generated source.
  1. Close the static-safety blind spots: typecheck tests/stories and the scripts/ tree, stop eslint-ignoring scripts/, pin react-icons exactly and reconcile the stale CLAUDE.md gotcha, and fix the disabled Button-as-link navigation/a11y bug.
