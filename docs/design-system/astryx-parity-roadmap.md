# Astryx-parity roadmap

**Goal:** reach at least **90/100** on every rescoped Astryx-comparison dimension, and make the selected utilities operational.

**Status source of truth:** this file (human) + [`scripts/design-system/astryx-roadmap.state.json`](../../scripts/design-system/astryx-roadmap.state.json) (machine). The guard [`scripts/design-system/check-astryx-roadmap.mjs`](../../scripts/design-system/check-astryx-roadmap.mjs) runs `npm run check:astryx-roadmap` and is wired into pre-push, so completed progress cannot silently regress.

Benchmark: [astryx.atmeta.com/components](https://astryx.atmeta.com/components) (Meta, React + StyleX, 160+ components, powers 13k+ apps). Prior roadmap: `docs/superpowers/specs/2026-07-03-astryx-level-design-system-roadmap.md`.

---

## Rescoped rubric (decided 2026-07-08)

DT is a portfolio-site design-system-of-record, not an app platform. "90" is measured against DT's **intended surface** and a **published, dogfooded package**, NOT against Astryx's absolute breadth or raw OSS adoption. The anti-goals below stay in force. Under this rubric all nine dimensions are reachable by engineering.

| Dimension | Now | Target | What earns 90 (rescoped, checkable) |
|---|---:|---:|---|
| Maturity | 82 | 90 | Versioned package + semver + changelog; ≥90% of catalog at `stable`; documented, frozen public API; no `alpha` in the shipped surface. |
| Breadth | 90 | 90 | Full coverage of the **intended-surface checklist** (below), all at `stable`. Not app-platform parity. |
| Scalability | 86 | 90 | Workspace package boundary; catalog decoupled from the app (0 `@/`, 0 direct `next/*`, i18n injected); multi-brand-capable token pipeline; scaffolder + contracts for contributors. |
| Futureproofness | 87 | 90 | Framework-agnostic package (Link/Image/Navigation injected), DTCG tokens, agent-manifest/MCP, current stack, bus-factor mitigations (docs + scaffolder + gates). |
| Accessibility | 81 | 90 | Every `stable` component: 4-mode AT + axe + forced-colors + reduced-motion verified; focus-trap/scroll-lock utilities operational for custom overlays; keyboard coverage documented. |
| Governance | 90 | 90 | Contract drift-as-build-failure (have), controls audit 100% (have), rhythmguard (have) + this roadmap self-check gate + published contract schema. |
| Agent-readiness | 90 | 90 | MCP server + CLI + agent-manifest + docs-registry all operational and documented ("agent-ready docs from CLI/MCP"). |
| Theming | 90 | 90 | Multi-brand theme generation from DTCG; light/dark/HC/forced-colors (have); documented theming API + generator/recipe. |
| Distribution | 80 | 90 | Published to a registry, versioned, installable, consumer-setup docs, dogfooded by the site via the package, documented second-consumer path. Not adoption count. |

Scores are tracked in the state file. Where a score is backed by a measurable fact, the guard ratchets it (coupling counts, stable count) so it cannot regress.

---

## Anti-goals (do not build; guarded by exact-name check)

App-platform surface DT will **not** build: **App Shell**, **Tree List**, **Data-grid/Table engine** (14-hook Astryx table), **Mega-menu nav**, and the full **Chat suite** (DT keeps its single ChatWidget; it will not grow Astryx's 15-part chat). The guard hard-fails if `AppShell`, `TreeList`, `DataGrid`, or `MegaMenu` appear as catalog components. Table/Chat are nuanced so they are prose anti-goals; add either only by amending this file deliberately, backed by a real use case.

---

## Intended-surface checklist (defines Breadth = 90)

Audited 2026-07-08 against the live catalog (159 contracts). Breadth-90 (rescoped)
= every intended-surface item **present**, and **stable where it has real
consumers**; a present-but-zero-consumer beta counts as covered (it cannot be
promoted, per the promotion policy). Legend: ✓ stable · β beta (present) · ✗ gap.

- Primitives: ✓Text ✓Title ✓Button ✓IconButton ✓Icon ✓Link ✓Badge ✓Label ✓Card ✓Divider ✓Avatar ✓StatusDot · βSpacer βKbd (0-consumer, covered).
- Forms: ✓TextInput ✓TextArea ✓Select ✓Combobox ✓MultiCombobox ✓Checkbox ✓CheckboxGroup ✓PhoneInput ✓FileUpload ✓FormField · βRadio βRadioGroup βSwitch (0-consumer) · ✗Slider (optional, deferred) · ✗**SegmentedControl → 3.2**.
- Feedback: ✓Toast ✓Spinner ✓Skeleton ✓AlertBanner ✓EmptyState ✓Progress · βTooltip (Tooltip 0-consumer, covered).
- Overlays: ✓Modal ✓Menu · Lightbox (catalog-exempt per CATALOG-POLICY) · ✗Popover (optional, deferred).
- Navigation: ✓Pagination ✓SkipLink ✓LanguageSwitcher ✓Breadcrumb ✓Tabs · site nav (patterns).
- Layout: ✓Container ✓Section ✓Grid ✓FlexBox ✓Stack · βCenter βAspectRatio (dev-harness-only, covered).
- Content/site: ✓List ✓CodeBlockWindow ✓CodeSnippet · Table (simple/static) not needed yet; the engine is an anti-goal · blog/marketing/work composites present.
- Authority additions: ✗**Command Palette → 3.1** (site/docs search + agent story).

**Breadth-90 reached (2026-07-10):** the 2 hard gaps are built (SegmentedControl 3.2, CommandPalette 3.1) and CodeSnippet — the last consumer-backed beta — is stable with a real Figma node (1161-2809). Every intended-surface item is present and stable where it has real consumers; the 0-consumer betas (Radio/RadioGroup/Switch/Tooltip/Kbd/Spacer/Center/AspectRatio) are covered-but-unpromotable by policy and do not block 90.

---

## Stopping contract (enforced by the guard)

An autonomous loop may drive this roadmap, but three tasks are **checkpoints** that require explicit human clearance and are marked 🔒 below. They are listed in `state.json` under `checkpoints`, and the guard **hard-fails** if any checkpoint task is marked `done` in `state.tasks` without an `approval: { clearedBy, on }` record. So the loop structurally cannot auto-complete them; it must stop, ask, and record the clearance.

- 🔒 **1.5 i18n decoupling** and **1.6 navigation decoupling** (`unverifiable-regression`): correctness across EN/FI/SV and routing/active-link behavior is not provable by the gates (English-only snapshots, no visual/multi-locale suite, CSS-module proxy hides mis-scoped classes). Needs browser + multi-locale review before merge.
- 🔒 **5.1 publish** (`authorization`): outward-facing and irreversible.

Everything else the loop may chain autonomously (branch, gate, PR, admin-merge, tick box, tighten ratchet) until all boxes are checked and all nine dimensions reach ≥90.

## Phased tasks

Legend: `[ ]` todo, `[~]` in progress, `[x]` done, 🔒 checkpoint (needs recorded clearance). Check the box AND update the state file when a task lands.

### Phase 0: Foundation & guardrails (the non-regression spine)
- [x] 0.1 Commit this roadmap + machine state as the durable source of truth.
- [x] 0.2 Declare anti-goals (above) and hard-guard the unambiguous ones.
- [x] 0.3 Build `check:astryx-roadmap` guard (anti-goals, operational-utility locks, coupling ratchets, stable floor); wire into pre-push.
- [x] 0.4 Seed coupling ratchets at today's values (41 `@/`, 15 `next/*`, 46 i18n, stable floor 58) so it is non-regressing from day one.
- [x] 0.5 Intended-surface checklist audited against the live catalog (159 contracts); ~93% present-coverage, 2 hard gaps (SegmentedControl 3.2, CommandPalette 3.1). Breadth re-baselined 44 → 66 under the rescoped rubric.

### Phase 1: Package boundary + decoupling → Maturity, Scalability, Futureproofness, Distribution
- [x] 1.1 Add the workspace root for package extraction (`packages/*`); the app intentionally remains at the repo root.
- [x] 1.2 Internalize `cn` / `@/lib/utils` into the DS boundary (moved to `nextjs-app/shared/lib/cn`, 55 imports repointed, app re-exports for back-compat); `catalogAppImports` 41 → 5.
- [x] 1.3 **Link Provider** → `nextjs-app/shared/lib/linkComponent.tsx` (context + `LinkProvider` + DS `Link` component that renders the injected link, default `<a>`); app injects next/link via `providers/NextLinkProvider`. All 9 catalog `next/link` sites swapped (pure import swap; the one server component works because `Link` is a client component, not a hook). `catalogNextImports` 15 → 13 (rest are next/image + next/navigation). Build-verified RSC boundary.
- [x] 1.4 Image provider → `nextjs-app/shared/lib/imageComponent.tsx` (context + `ImageProvider` + DS `Image` + `ImageSource` type; default `<img>` maps fill/priority/sizes, drops next-only props); app injects next/image via `providers/NextImageProvider`. All 12 catalog `next/image` sites swapped (10 render + 2 type-only). `catalogNextImports` 13 → 6 (remainder is next/navigation, 1.6 checkpoint). In-app is a pass-through; a11y tree unchanged (img role + alt).
- [x] 🔒 1.5 i18n decoupling: injected translator/localization adapter with English defaults is implemented for publishable shared components + patterns; `catalogI18nImports` and `sharedPackageI18nImports` are ratcheted to 0. **Cleared 2026-07-08 by Petri Lahdelma after multi-locale browser/package review.**
- [x] 🔒 1.6 Remove remaining `@/` and `next/navigation` from the catalog: navigation adapter + Next App Router provider implemented, `catalogAppImports` and `catalogNextImports` are ratcheted to 0. **Cleared 2026-07-08 by Petri Lahdelma after package navigation behavior review.**
- [x] 1.7 `@digitaltableteur/tokens` + `@digitaltableteur/tokens-css` packages from the existing token pipeline: private package sources, package metadata, generated ESM/DTCG/Tailwind exports, theme-complete token CSS projection, and `check:token-packages` guard.
- [x] 1.8 Library build: `@digitaltableteur/react` workspace package compiles with Vite, emits `dist/index.js`, `dist/index.d.ts`, and `dist/style.css`; `react`/`react-dom` are peer deps; browser-heavy renderers are package dependencies/externalized; `check:react-package` verifies the Next-compatible `"use client"` bundle, Node ESM import smoke, and npm pack dry-run.
- [x] 1.9 Site consumes `@digitaltableteur/react` via the registry package: app/provider/lib imports use the npm package name, `app/layout.tsx` imports `@digitaltableteur/react/style.css`, `siteDtImports` is ratcheted to 0, and the Next production build is green.
- [x] 1.10 Local package dogfood browser smoke: `check:site-package-dogfood` verifies representative package-consumed app routes across EN/FI/SV, including contact/home copy, WorkNav/BlogNav translations, SiteHeader link routing + active state, WorkNav/BlogNav button routing, selected-work image loading on direct home load and client navigation back home, toast-only content-language notices, and the PSEO Breadcrumb production consumer, then writes `.omx/state/design-system/site-package-dogfood/latest.json`. This strengthens the 1.5/1.6 checkpoint evidence but does not clear those human approvals.
- [x] 1.11 i18n/navigation visual review packet: `test:stories:i18n` now runs a real Playwright matrix against the package-consumed app, covering EN/FI/SV desktop+mobile screenshots for home/contact/work/blog plus desktop SiteHeader, WorkNav, and BlogNav routing flows. It writes `.omx/state/design-system/i18n-navigation-review/latest/{report.json,review.md,screenshots/}` for human checkpoint review. This creates repeatable visual evidence for 1.5/1.6 but still does **not** clear the approvals.

### Phase 2: Curated utilities operational → Accessibility, Futureproofness ("selected utilities operational")
- [x] 2.1 `useMediaQuery` — SSR-safe canonical primitive at `nextjs-app/shared/hooks/useMediaQuery.ts` (+ test); operational. Migrated the one genuine responsive-breakpoint site (SocialShare `(width < 768px)`). The other `matchMedia` sites are `prefers-reduced-motion` (owned by `useHydrationSafeMotion`/`motion-safe`) and `prefers-color-scheme` (ThemeProvider), left intentionally; new responsive checks use this hook.
- [x] 2.2 `useFocusTrap` — extracted Modal's inert-background + focus-first + restore logic into `nextjs-app/shared/hooks/useFocusTrap.ts` (+ test); Modal consumes it (behavior-identical, 40 Modal tests green). Operational.
- [x] 2.3 `useScrollLock` — `nextjs-app/shared/hooks/useScrollLock.ts` (+ test), restores previous overflow so nested locks compose. Wired into Modal (closed a real gap: Modal did not lock background scroll). Operational.
- [x] 2.4 `LinkProvider` operational (built in 1.3); documented in the Utilities Storybook page as part of 2.6.
- [x] 2.5 `useTheme` / `ThemeProvider` confirmed public (exported from the `@dt` barrel) and locked in state.
- [x] 2.6 Utilities documented in Storybook: `nextjs-app/shared/foundations/05-Utilities.mdx` (`Overview/05-Utilities`) covering useMediaQuery / useFocusTrap / useScrollLock / LinkProvider / useTheme, plus the not-adopted rationale.
- [x] 2.7 `LayerProvider` / `useLayer` operational: `nextjs-app/shared/lib/layer.tsx` (+ test) resolves host-injected portal containers and standard layer z-index tiers; `Modal` now consumes it for its portal target while preserving the default `document.body` behavior.
- [x] 2.8 `useOverflow` / `useScrollOverflow` operational: `nextjs-app/shared/hooks/useOverflow.ts` (+ test) centralizes overflow and scroll-affordance measurement; `NewsBulletin` now consumes `useScrollOverflow` for its horizontal previous/next affordances.
- [x] 2.9 `useStreamingText` operational: public package hook with adaptive burst smoothing, Unicode grapheme safety, reduced-motion/instant bypass, and stream-end snap. Donny consumes it only for the active assistant reply; the message log stays `aria-busy` until completion.
- Deferred (adopt only when a trigger component needs them): Syntax Theme, useClickableContainer, useEntryAnimation, useInputContainer, useListFocus, useKeyboardHint. Skipped (app-platform only): useGridFocus, useTreeFocus, Media Theme, useImageMode.

### Phase 3: Targeted breadth → Breadth (rescoped)
- [x] 3.1 CommandPalette (new, **alpha**): portal `role=dialog aria-modal` with a `role=combobox` filter input (aria-controls + aria-activedescendant) over a `role=listbox`; Arrow/Enter/Escape keyboard, focus trap + scroll lock (reuses `useFocusTrap`/`useScrollLock`), label/placeholder/emptyText as props (English defaults, no i18n coupling). Full 5-file set + 8-test suite (render/filter/keyword/keyboard/enter/escape/click/axe). Closes the last hard breadth gap. breadth 70 → 74. stableCount untouched (alpha).
- [x] 3.2 SegmentedControl (new, **alpha**): single-select `role=radiogroup` with roving-tabindex arrow/Home/End nav, sm/md/lg, CSS Modules + tokens (light/dark/HC/forced-colors/reduced-motion), full 5-file contract + spec + stories + test (render/click/keyboard/disabled/axe). Closes a hard breadth gap. breadth 66 → 70. stableCount untouched (alpha).
- [x] 3.3 Intended-surface gaps closed (SegmentedControl, CommandPalette built) and the last consumer-backed beta promoted: CodeSnippet's Figma component set now exists in DT-Site-stuff (Molecules, node 1161-2809: Inline/Single/Multi variants composed from Button/SplitButton instances with DT color/dimension token bindings), its contract is `stable` with 4-mode accessibility-tree snapshots matching the StatusDot convention, and CodeSnippet plus its prop types are exported from `@digitaltableteur/react` (public API manifest updated; ships with the next published version). 0-consumer betas are covered-but-unpromotable by policy. `check:beta-promotion-readiness` writes a candidate report, blocks premature `stable` flips, and records which candidates still lack production consumers, Figma nodes, or snapshot evidence. Breadcrumb, Tabs, Progress, AlertBanner, and EmptyState cleared the bar earlier. **Breadth → 90.**
- [x] 3.4 Breadcrumb production consumer established and promoted: `PseoLeafPageView` now consumes the package `Breadcrumb` instead of hand-authored `next/link` breadcrumb markup, `check:site-package-dogfood` verifies the EN PSEO leaf trail links/current item through the package boundary, and `check:beta-promotion-readiness` verifies production consumer, a11y snapshots, forced colors, and real Figma evidence before allowing stable status.
- [x] 3.5 EmptyState production consumer established and promoted: `WorkGrid` now renders the package `EmptyState` for its no-projects fallback using the existing `workNoResults*` translations, `check:beta-promotion-readiness` verifies production consumer, a11y snapshots, forced colors, and real Figma evidence before allowing stable status, and the clean npm consumer smoke imports/types/renders `EmptyState` from `@digitaltableteur/react`.
- [x] 3.6 AlertBanner production consumer established and promoted: `Mermaid` now renders the package `AlertBanner` for persistent diagram-render errors using EN/FI/SV `mermaidErrorTitle` translations while leaving successful diagram output unchanged. `check:beta-promotion-readiness` verifies production consumers, a11y snapshots, forced colors, and real Figma evidence before allowing stable status, and the clean npm consumer smoke imports/types/renders `AlertBanner` from `@digitaltableteur/react`.
- [x] 3.7 Tabs production consumer established and promoted: `ContactInquiryPanel` now renders the package `Tabs` for the production contact message/book switcher, using `ariaLabel` to preserve the specific `contactInquiryModeLabel` accessible name and `getTabPanelProps` for the paired panels. `check:beta-promotion-readiness` verifies production consumers, a11y snapshots, forced colors, and real Figma evidence before allowing stable status, and the clean npm consumer smoke imports/types/renders `Tabs` plus `getTabPanelProps` from `@digitaltableteur/react`.
- [x] 3.8 Progress production consumer established and promoted: `ContactInquiryPanel` now renders the package `Progress` as the indeterminate booking-calendar loading fallback passed into `DonnyBookingEmbed`, with EN/FI/SV `donnyBookingLoading` translations and a default embed fallback for direct consumers. `check:beta-promotion-readiness` verifies production consumers, a11y snapshots, forced colors, and real Figma evidence before allowing stable status, and the clean npm consumer smoke imports/types/renders `Progress` from `@digitaltableteur/react`.
- [x] 3.9 CodeSnippet production consumer and a11y evidence established, intentionally still beta: `RhythmguardPage` now uses `CodeSnippet` for a real copyable strict-config install snippet, Storybook Vite explicitly prebundles the Prism modules CodeSnippet imports so the story iframe no longer hangs on first render, and `CodeSnippet/__a11y-snapshots__` now contains Default, Example, and real Playwright forced-colors snapshots. `check:beta-promotion-readiness` now reports CodeSnippet blocked only by `realFigmaNode`; do not promote or export it until the contract points at a real numeric Figma node.

### Phase 4: Governance + agent-readiness + theming → those three to 90
- [x] 4.1 Multi-brand theme generation: `scripts/design-system/generate-theme.mjs` (+ test, `npm run generate:theme`) emits a scoped brand override block validated against the token catalog (unknown token = hard error) into stylelint-ignored `dist/themes/`; demo `acme` brand + `03-Theming` doc section. On top of existing light/dark/HC/forced-colors. **theming → 90** (3rd dimension at target).
- [x] 4.2 Agent-readiness verified + documented: `07-Agent-Readiness.mdx`. `find-component` CLI (intent-ranked) and `ds:mcp` stdio MCP server (`digitaltableteur-design-system`, tools+resources+docs-registry) both smoke-tested working; dist artifacts (agent-manifest 158 comps, docs-registry, agent-blocks, zod catalog) + `agent:eval` harness documented. **agentReadiness → 90** (2nd dimension at target).
- [x] 4.3 Contract schema exposed + documented: `06-Governance.mdx` (`Overview/06-Governance`) documents the versioned schema (public `$id`), the drift-as-build-failure gate stack, and the agent inputs. Schema was already published via its public `$id`; now discoverable. **governance → 90** (first dimension at target).

### Phase 5: Distribution → Distribution to 90
- [x] 🔒 5.1 Publish `@digitaltableteur/tokens` → `@digitaltableteur/tokens-css` → `@digitaltableteur/react` (dep order) to a registry. `@digitaltableteur/tokens@0.1.0`, `@digitaltableteur/tokens-css@0.1.0`, and `@digitaltableteur/react@0.1.1` are published as private/restricted packages. `check:registry-token-install`, `check:package-registry-status`, and `check:react-registry-install` verify registry access and installability. **Remaining roadmap work:** the next React patch should publish through the committed OIDC Trusted Publisher path rather than manual npm publish.
- [x] 5.2 Consumer-setup docs + documented second-consumer path.
- [x] 5.3 Site dogfoods the registry React package (`@digitaltableteur/react` + package stylesheet). Token packages are also root registry dependencies while the runtime CSS source remains `variables.css` for 1:1 continuity.
- [x] 5.4 Local registry-shape install smoke: `check:npm-consumer-install` packs all three npm packages, installs the tarballs into a clean temporary consumer, resolves token JS/JSON/CSS/theme exports, resolves React CSS, imports the public adapters/chrome patterns, SSR-renders a Button from the installed package, compiles a strict TypeScript consumer against the emitted declarations, and writes `.omx/state/design-system/npm-consumer-install/latest.json`. This is the local proof for the future private-registry React package dogfood step.
- [x] 5.5 Published-token registry install smoke: `check:registry-token-install` installs `@digitaltableteur/tokens@0.1.0` and `@digitaltableteur/tokens-css@0.1.0` from the private npm registry into a clean temporary consumer, verifies token JS/JSON/Tailwind/CSS/theme exports from `node_modules`, and writes `.omx/state/design-system/registry-token-install/latest.json`.
- [x] 5.6 React publish preflight gate: `check:react-publish-preflight` aggregates lint, typecheck, full Vitest, Storybook static build, Next production build, roadmap ratchets, package release metadata, Trusted Publisher readiness, private token registry install, React registry-smoke waiting state, local tarball consumer install, package tarball contents guard, package publish dry-run guard, React public surface guard, React public API guard, package dogfood browser checks, the i18n/navigation review packet, and the consolidated React publish review packet, then writes `.omx/state/design-system/react-publish-preflight/latest.json`. `check:react-publish-ready` is the strict final-publish mode and fails until 1.5, 1.6, explicit `@digitaltableteur/react` publish clearance, and a publishable public surface are recorded; the approval handoff lives at `.omx/state/design-system/react-publish-clearance/handoff.md` and includes a local evidence snapshot for the latest i18n/navigation matrix plus package dogfood report.
- [x] 5.7 React public surface maturity guard: `check:react-public-surface` reports the package's public contract exports, requires every `stable` component/pattern contract to be exported, and ratchets alpha exposure at **zero**. `CommandPalette` and `SegmentedControl` remain internal catalog alpha components until promotion evidence exists, and are not part of the `@digitaltableteur/react` public surface. Strict mode (`--require-publishable`, wired into publish-ready after clearance) also requires zero alpha contract exports, matching the Maturity rubric's "no alpha in the shipped surface" requirement.
- [x] 5.8 React public API freeze: `check:react-public-api` compares the built package runtime exports and package subpath exports against `packages/react/public-api.manifest.json`. The manifest currently freezes 160 runtime exports plus the `.` and `./style.css` entrypoints; intentional public API changes require `--update`. `check:react-package` and the React publish preflight both run this guard.
- [x] 5.9 Package release metadata guard: `check:package-release-notes` verifies semver, restricted npm publish metadata, GitHub repository metadata for npm Trusted Publisher matching, packed `dist`, required package exports, and version-matched `CHANGELOG.md` entries for `@digitaltableteur/tokens`, `@digitaltableteur/tokens-css`, and `@digitaltableteur/react`. React remains unpublished; its changelog records the prepared 0.1.0 surface and the open human checkpoints.
- [x] 5.10 React publish review packet: `check:react-publish-review` validates the latest i18n/navigation report and screenshots, package-consumed app dogfood report, React public API report, public-surface maturity report, package release metadata, package tarball contents, package publish dry-runs, Trusted Publisher readiness, local tarball consumer install evidence, published-token registry install evidence, and the React registry install smoke state, then writes `.omx/state/design-system/react-publish-review/latest.{md,json,html}` for human review. The HTML contact sheet lays out the EN/FI/SV screenshots, navigation checks, browser dogfood checks, and package publish readiness evidence. It does **not** clear 1.5, 1.6, or 5.1 approvals.
- [x] 5.11 Package tarball contents guard: `check:package-tarballs` runs `npm pack --dry-run --json` for `@digitaltableteur/tokens`, `@digitaltableteur/tokens-css`, and `@digitaltableteur/react`, verifies required files, records entry/size ceilings, and fails on source/config/test/story/env/media leakage outside the intended `dist` + package metadata payload.
- [x] 5.12 Stable contract npm-surface coverage: `@digitaltableteur/react` now exports every `stable` component/pattern contract (`63/63` after Breadcrumb, EmptyState, AlertBanner, Tabs, and Progress promotion) and `check:react-public-surface` fails if a stable contract is missing from the package surface. This closes the pre-publish gap where the API was frozen but did not yet prove the complete stable design-system surface was served from npm.
- [x] 5.13 Trusted Publisher readiness: each package manifest now carries the GitHub repository metadata npm requires for Trusted Publisher matching, `.github/workflows/ds-publish.yml` is the manual GitHub Actions OIDC publish transport, and `check:trusted-publisher` verifies the workflow is manual-only, token-free, uses Node/npm versions compatible with npm Trusted Publishing, writes `.omx/state/design-system/trusted-publisher/handoff.md` with the exact npm form values plus repository commit-scope table, and writes `publish-pathspec.txt` plus `excluded-dirty-paths.txt` so the eventual publish transport commit can be staged from a precise path list without sweeping in unrelated local work. The commit scope includes package manifests, publish guards, token build scripts, token catalog/DTCG inputs, `variables.css`, React package sources, public navigation pattern sources, and host adapter utilities, because GitHub Actions rebuilds token and React packages from repository source. `check:react-publish-clearance` also writes the local review-evidence snapshot and warnings into its handoff while keeping GitHub Actions as publish transport only. Local machines remain the CI-quality authority; GitHub Actions is only the npm OIDC transport.
- [x] 5.14 Package publish dry-run guard: `check:package-publish-dry-run` runs `npm publish --dry-run --access restricted --json` from each package directory for `@digitaltableteur/tokens`, `@digitaltableteur/tokens-css`, and `@digitaltableteur/react`, verifies the publish id/integrity/shasum plus required package files, and writes `.omx/state/design-system/package-publish-dry-run/latest.json`. The manual GitHub Actions OIDC workflow runs this guard before any dry-run or real publish.
- [x] 5.15 Package registry status guard: `check:package-registry-status` verifies the current external npm state: token packages and the current React package version are published with private package status and org `read-write` access. Strict publish-ready mode requires the prepared next React version to be unpublished so duplicate publishes fail before OIDC.
- [x] 5.16 React post-publish registry install smoke: `check:react-registry-install` installs the exact private `@digitaltableteur/react` version from npm into a clean consumer, resolves `@digitaltableteur/react/style.css` plus token CSS from `node_modules`, SSR-renders representative stable components, compiles a strict TypeScript consumer, and writes `.omx/state/design-system/react-registry-install/latest.json`. The GitHub OIDC workflow runs it after a non-dry-run React publish; strict pre-publish verification uses `--expect-unpublished` for the next patch version.
- [x] 5.17 Registry resolution guard: root `package.json` now consumes `@digitaltableteur/react`, `@digitaltableteur/tokens`, and `@digitaltableteur/tokens-css` from the private npm registry instead of local workspace symlinks. `check:package-registry-resolution` fails if any of the three packages resolve from `packages/*`, if package-lock contains workspace links, or if `node_modules/@digitaltableteur/*` is symlinked.
- [x] 5.18 Storybook registry package guard: `check:storybook-registry-package` verifies Storybook can compile a smoke story that imports `@digitaltableteur/react`, `@digitaltableteur/react/style.css`, and token CSS from registry `node_modules` paths, and fails if Storybook aliases `@digitaltableteur/*` back to local `packages/*`. The React publish preflight now runs this guard after the static Storybook build.

---

## How to update (every session)

1. Do the task; run its gates.
2. Tick the box here; update `astryx-roadmap.state.json` (tighten ratchets, flip a utility to `operational`, bump a dimension `current`).
3. Run `npm run check:astryx-roadmap` (also runs on pre-push). Green means no completed item regressed.

## Known caveats (carried from the feasibility analysis)

- The gates cannot fully prove "zero regressions" for future i18n/navigation changes: AT snapshots are English-only and a11y-tree-only, the vitest CSS-module proxy hides mis-scoped classes, and CI is quota-dead. `check:site-package-dogfood`, `test:stories:i18n`, and `check:react-publish-review` remain the required local-machine evidence stack before touching those boundaries again.
- The first React package publish is complete, but the next React patch still needs to go through the committed GitHub Actions OIDC Trusted Publisher path to prove that transport end to end.
