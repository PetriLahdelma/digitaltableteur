# Claude Code automation recommendations

> Generated by running the [claude-code-setup](https://github.com/anthropics/claude-plugins-official/tree/main/plugins/claude-code-setup) plugin's `claude-automation-recommender` skill against this repo (May 2026).

**Plugin status:** installed at project scope (`claude-code-setup@claude-plugins-official`)

**Re-run anytime in Claude Code:**

```
recommend automations for this project
```

---

## Codebase profile

| Signal | Detected |
|--------|----------|
| **Type** | Hybrid monorepo — Next.js 16 production + Vite legacy + Storybook |
| **Stack** | React 19, TypeScript 6.x, Vitest, Playwright, Sanity CMS |
| **Backend** | Next.js API routes, MongoDB, Vercel AI SDK (chat) |
| **Design system** | 80+ contracted components, CSS Modules, agent-manifest |
| **CI** | GitHub Actions PR validation (typecheck, lint, visual, security) |
| **Agent docs** | Three-layer system: router → area AGENTS.md → dt-* skills |

---

## Already strong (no action needed)

| Category | What you have |
|----------|---------------|
| **MCP** | Figma, Sentry, Vercel, GitHub, Sanity, Context7, Docker, next-devtools |
| **Skills** | `dt-design-system`, `dt-nextjs-app`, `dt-api-routes`, `dt-scripts`, `dt-sanity-cms`, `add-project` |
| **Subagents** | accessibility-expert, **security-reviewer**, seo-expert, test-runner, QA-lead, translation-language-checker, GSD agents |
| **Hooks** | Prettier on TS/CSS edit; blocks `rm -rf` / `--force`; **blocks `.env*` / mcp.json edits** |
| **Plugins** | frontend-design, figma, superpowers (user scope) |

---

## Implemented (May 2026)

### 1. Block `.env` and secret file edits ✅

**Where:** `.claude/hooks/block-secret-files.sh` + PreToolUse hook in `.claude/settings.json`

Blocks edits to `.env*`, `mcp.json`, `credentials.json`, `.pem`, `.key` unless user disables hook.

### 2. `dt-ship-pr` skill ✅

**Where:** `.claude/skills/dt-ship-pr/SKILL.md` (user-only via `disable-model-invocation: true`)

Trigger: "create PR", "ship this", "open pull request"

### 3. `security-reviewer` subagent ✅

**Where:** `.claude/agents/security-reviewer.md`

Invoke after changes to `app/api/chat/`, guardrails, or `tests/security/`.

---

## Remaining recommendations

### MCP servers

#### Playwright MCP (if not in Cursor yet)

**Why:** Heavy Playwright usage (`test:visual`, Storybook runner, a11y snapshots). Browser MCP complements `agent-browser` for interactive debugging during agent sessions.

**Install (Claude Code):**

```bash
claude mcp add playwright
```

**Cursor:** enable browser MCP in Cursor settings if available.

---

### Plugins

#### hookify (optional)

**Why:** Team already uses hooks in `.claude/settings.json`. The [hookify plugin](https://github.com/anthropics/claude-plugins-official) helps author hook rules interactively when adding new guardrails (e.g. the `.env` block above).

**Install:**

```bash
claude plugin install hookify@claude-plugins-official --scope project
```

---

## Gaps vs recommender defaults — already covered

| Recommender default | Your equivalent |
|--------------------|-----------------|
| context7 MCP | ✅ in `mcp.json` |
| frontend-design skill | ✅ user plugin + `dt-design-system` |
| new-component skill | ✅ `npm run new-component` + `dt-design-system` |
| GitHub MCP | ✅ configured |
| Sentry MCP | ✅ configured |
| Auto-format hook | ✅ Prettier PostToolUse |

---

## Suggested next steps (priority order)

1. **Evaluate Playwright MCP** — if browser automation gaps remain after agent-browser
2. **Optional: hookify plugin** — interactive hook authoring for future guardrails

---

## Maintenance

- Re-run recommender after major stack changes (e.g. Next 16 migration)
- Plugin install command: `claude plugin install claude-code-setup@claude-plugins-official --scope project`
- Update this doc when implementing recommendations above

**Want more?** Ask for deeper recommendations in any category (e.g. "more hook options" or "MCP servers for SEO workflow").

**Want help implementing?** Ask to implement any item from this list.
