- status: Work in progress (beta)
- figure:
  - text: typescript
  - button "Copy code to clipboard": Copy
  - button "More copy options": ▾
  - status
  - region "Code snippet in TypeScript":
    - code: "import { createGatewayProvider } from \"@ai-sdk/gateway\"; const provider = createGatewayProvider({ baseURL: process.env.AI_GATEWAY_URL, apiKey: process.env.AI_GATEWAY_API_KEY, }); export async function ask(question: string) { const model = provider(\"openai/gpt-4o-mini\"); const result = await model.doStuff({ prompt: question }); return result.text; }"