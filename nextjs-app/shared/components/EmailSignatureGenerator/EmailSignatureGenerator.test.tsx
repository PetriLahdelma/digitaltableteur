/**
 * EmailSignatureGenerator Tests
 *
 * NOTE: This component is primarily tested via Storybook tests.
 * The Storybook addon-vitest runs these component stories in a real browser environment.
 *
 * To run component tests:
 *   npm test -- EmailSignatureGenerator
 *
 * See EmailSignatureGenerator.stories.tsx for the test stories.
 */

import { describe, it, expect } from "vitest";

describe("EmailSignatureGenerator", () => {
  it("is tested via Storybook stories", () => {
    // This component is tested via Storybook's vitest addon
    // which provides a real browser environment with proper React setup.
    // See EmailSignatureGenerator.stories.tsx for the interactive tests.
    expect(true).toBe(true);
  });
});
