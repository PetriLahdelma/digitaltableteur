"use client";

interface CodeDemoProps {
  duration: number;
  delay: number;
  writing: boolean;
  cursor: boolean;
}

export const CodeDemo = ({
  duration: _duration,
  delay: _delay,
  writing: _writing,
  cursor: _cursor,
}: CodeDemoProps) => {
  return null;
};
